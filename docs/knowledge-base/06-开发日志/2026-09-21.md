---
title: 2026-09-21
stage: 3
status: current
updated: 2026-09-21
tags:
  - 羽毛球赛事管理系统
  - 开发日志
---

# 2026-09-21 开发日志

## 本日范围

执行外部源码审查包 `requirements/badminton-source-review-v3/` 的 R3-001—R3-005 修复与裁判页视觉收敛。
起始 HEAD `70fff5e`（比审查基线 `84a1592` 新一个提交），起始工作区除未跟踪的 `requirements/` 外干净。
先比较本机与审查基线差异：五项发现在本机全部仍然成立，没有重复修已解决的问题，也没有回退任何代码。

## 实际做了什么

1. 先追加红灯回归再动实现。`tests/domain/threshold-obligations.test.ts` 在修改实现前为 10 红 4 绿。
2. R3-001：阈值义务改为按本局记录一次性触发，阈值全部来自冻结规则快照；引擎升到 v2，历史事件不改写、不删除，重放按事件记录的引擎形状比对。
3. R3-002：新增 `src/ui/authoritative-snapshot.ts`，界面快照采用改为统一的单调版本策略；命令核销与快照采用彻底分开。
4. R3-003：新增 `src/ui/command-outcome.ts` 与 `src/ui/pending-command-store.ts`，区分「可核对的明确拒绝」与「结果未知」；本机存储全部容错并进入受控恢复。
5. R3-004：`endedAt` 只在跨越「未结束 → 已结束」时写入，提交与复核不再覆盖实际结束时刻。
6. R3-005：新增 `src/ui/secure-command-id.ts`，进入可写界面前检测安全随机源；非安全上下文使用 `crypto.getRandomValues` 兼容路径并常驻 HTTPS 提示，未使用 `Math.random`。
7. 性能追加核验：量测后对轮询路径做最小优化，行锁、重放和哈希校验都没有删除。
8. 裁判页做两轮聚焦收敛，没有新增第二套 UI，也没有重写 `CourtConsole`。

## 本日结果

lint、typecheck、build 均退出码 0；`pnpm test` 153/153；Playwright 47/47；生产启动安全 4/4；同步脚本 10/10。
截图 `artifacts/referee-court-v2/v3-before` 与 `v3-after` 各 13 张，上一轮的 `before/`、`after/` 没有被覆盖。

## 本日边界

无 Prisma 迁移，未改 `prisma/seed.ts`，未连接生产数据库，未提交、未推送、未部署。
本日结果全部是开发自测，不是独立验收。真机执裁、HTTPS 部署、并发负载和 BWF 原文采用仍未做。

## 本日发现的既有问题（未擅自修改）

`tests/e2e/phase-3.spec.ts` 每次运行都无条件重写 `artifacts/phase3-browser-simulated/` 的 6 张截图但不更新 `SHA256SUMS`。
把这 6 个文件还原为 HEAD 提交字节后校验仍 6/6 FAILED，说明**本轮开始之前**清单就已经与已提交 PNG 不一致。
本轮已还原为 HEAD 字节、未留下本轮生成图、未擅自改写清单，交由用户决定处理方式。

---

# 第二轮：70fff5e 外部源码复审的定向修复（RV3-001—RV3-008）

审查基线仍是 `70fff5e`，与本机 HEAD 相同；工作树带着上一轮 R3 修复的未提交改动。
审查方读了固定提交的源码，但没有在项目环境跑过任何测试，附带测试只做过语法检查。
本轮先把草案逐字放进 `tests/domain/referee-boundary-regressions.test.ts` 跑出真实结果，再动实现。

## 逐项结论

- RV3-001（阈值重复）：**草案 6 条一次全绿**——已由上一轮 R3-001 修复覆盖，不重复修、不回退。
- RV3-005 / RV3-007 / RV3-008：同样已在上一轮以 R3-002 / R3-004 / R3-003 修复，本轮只复核不重做。
- RV3-003（暂停中清空待办后卡死）：**真实复现**，3 个规则档全红，已修。
- RV3-006（更正照搬旧待办）：**真实复现**，已改为与服务端共用同一份推导。
- RV3-002（抛币负方选择写死）：**真实复现**，改为分步联动表单。
- RV3-004（局末/赛末无撤销入口）：**真实复现**，在结束待提交相位放开得分方的撤销入口。

## 实现要点

`RESUME_MATCH` 按当前待办结算恢复相位——刻意不改待办处理命令的输出，
这样历史事件的前后快照逐字不变，`replayMatch` 不受影响。
`deriveCorrectedObligations` 成为更正推导的唯一来源，界面调用它而不是自写一套。
抛币表单没有随机数，取消不提交，确认前不发命令。
局末撤销只放开得到结束分的一方，是否真能撤销仍由服务端 `applyUndo` 判定。

## 历史兼容核对

只读查询两个本机库：`MatchEvent` 计数均为 0，`PAUSE_MATCH`/`RESUME_MATCH` 各 0 条。
没有删事件、没有清库、没有关闭哈希或重放校验，`MATCH_ENGINE_VERSION` 本轮不变。

## 上一轮遗留的截图清单问题（本轮按用户授权处理）

两个 e2e spec 默认改写到未提交的 `*local-run` 目录并加入 `.gitignore`，
只有显式设置 `PHASE3_SHOT_DIR` / `REFEREE_SHOT_DIR` 才写入已记账目录。
`SHA256SUMS` 按当前已提交字节重算，自校验 6/6 OK；原清单保留为 `SHA256SUMS.superseded-2026-09-21`，
同目录新增 `README.md` 说明经过。重算只是让清单与现存文件自洽，不代表复现了旧清单的字节。
复跑整套 e2e 后 `git status artifacts/` 不再出现被修改的已提交截图。

## 本日第二轮结果

lint、typecheck、build 均退出码 0；`pnpm test` 19 文件 178/178；Playwright 52/52；
生产启动安全 4/4；同步脚本 10/10。
界面截图 `artifacts/review-70fff5e/shots/`：抛币表单与局末撤销入口各 5 个视口，均无根级横向溢出。

## 本日第二轮边界

无 Prisma 迁移，未改 `prisma/seed.ts`，未连接生产数据库，未提交、未推送、未部署。
未进入阶段 4，未开始报名编排。全部是开发自测，不是独立验收。
真机执裁、HTTPS 部署、并发负载和 BWF 原文采用仍未做。

## 公网 HTTP 演示部署

- 按用户授权把当前阶段 3 收尾版本部署到公网演示服务器的独立部署目录，只公开 TCP 80；同机既有项目的端口、容器和数据卷均未修改。
- 使用 Node 24.21.0、pnpm 11.9.0、Next.js 16.3.5 与 PostgreSQL 15 构建；Prisma 3 条迁移成功，生产镜像 SHA-256 为 `63c7664b935230100aa8ccdc3e1b6491224b2a4c685e0bfce1b07ac7d81017cb`。
- 种子幂等写入 3 个模拟身份、6 名匿名选手、1 个赛事、2 个项目、3 场比赛、3 条赛事角色和 2 条裁判指派。参赛选手账号仅为无赛事角色的认证只读身份，未与 `Participant` 建立业务绑定。
- 公网 `/api/health`、首页和同机既有项目首页均返回 200；管理员、裁判员、参赛选手三套账号分别完成登录 200、会话 200、对应页面 200；应用容器重启后再次健康。
- 运行容器未携带 `DEMO_ADMIN_*`、`DEMO_REFEREE_*` 或 `DEMO_PARTICIPANT_*`；模拟口令不进入仓库或知识库。
- 生成 root-only 初始数据库快照并通过 `gzip -t` 与 SHA-256；未做恢复演练。部署仍为明文 HTTP 直连 Next.js，HTTPS、域名、反向代理、真机跨设备登录和正式生产验收未完成。
- 部署收口后的本地回归为 lint、typecheck、Vitest 179/179、生产启动安全 4/4、知识库同步脚本 10/10；本轮未重跑 Playwright，最近一次全量仍为 52/52。

## 人工直采模拟数据与只读比赛看板

- 按用户手动验证要求扩充幂等种子到 16 名匿名选手和 13 场比赛：7 场待开赛、1 场结束待提交、1 场已提交待复核、4 场结果已锁定，包含正常两局比分和 `RET`。
- 已完成结果和待处理结果全部经真实计分命令、结果提交与裁判长复核服务生成；6 个结果样本均可从 55 条生产事件中的对应子集确定性重放，没有直接篡改比分字段。
- 新增公开只读 `/board`，按正在进行、赛后待处理、待开赛和已完成分组，每 15 秒刷新；页面明确提示实际写入必须从裁判账号的“我的执裁”进入。
- 本地门禁为 lint、typecheck、build、Vitest 20 文件 182/182、Playwright 53/53、生产启动安全 4/4；1440×1000 与 390×844 浏览器实拍无根级横向溢出。
- 上线前生成 root-only 快照 `pre-manual-board-20260921.sql.gz`，上线后生成 `manual-board-after-20260921.sql.gz`；两者 `gzip -t` 和 SHA-256 通过。最终镜像 `campus-badminton-app:20260921-manual-board2`（SHA-256 `82d04894…3a78e`）健康运行。
- 公网 `/board` 与公开 API 均 200，13 场数据可见；管理员、裁判员、参赛选手三类账号均登录 200 并进入对应页面；同机既有项目首页仍 200、容器 healthy。
- 这次是阶段 3 人工测验数据和只读看板，不进入阶段 4，不声称完成报名、编排、排名、成绩册或正式公开发布；未提交、未推送。
- 部署卫生复查发现首次 board1 镜像误带本机 `.env.seed`。运行容器未加载该文件，且两套本机口令与服务器口令摘要比较均不同。随后以排除所有 `.env*` 的 board2 重建；容器文件系统、镜像环境变量、逐层 tar 成员扫描全部 0 命中。切换健康后定向删除旧镜像、旧压缩包和隔离副本；未执行会影响其他项目的全局 Docker prune。

## 人工测试零起点比赛补充

- 用户反馈裁判端看到的都是已结束比赛。生产库核对显示原有 6 场比赛虽为版本 0、事件 0、0:0，但历史比赛按时间排在前面且命名不直观，导致人工测试入口不清楚。
- 新增 `MS-FRESH-001/002`、`MD-FRESH-001/002` 四场全新比赛；每场均由生产 SQL 逐项确认 `READY`、版本 0、事件 0、首局 0:0 未完成、1 名活动裁判。
- “我的执裁”把 `READY + version=0` 排在其他比赛之前，并显示“全新 · 0:0”；生产 HTML 中 `MS-FRESH-001` 位置早于 `MS-LIVE-001`。看板同步标为“全新 0:0 / 可直接开赛”。
- 本地 lint、typecheck、build、Vitest 20 文件 183/183、Playwright 54/54、生产启动安全 4/4 通过。公网看板为 200、总数 17 且四个 FRESH 编码齐全；同机既有项目仍为 200。
- 更新前备份 `<部署目录>/backups/pre-fresh-zero-20260921.sql.gz` 的 SHA-256 为 `d381b9a1fd684276cca3852a0093af2e86e8c50427747e1bd69e1e3b4202ca07`；更新后备份 `fresh-zero-after-20260921.sql.gz` 为 `154ef6bd017294daefc5e1377826e704b69b3a3f3d95c06d995387540c3ea812`，均通过 `gzip -t`。
- 最终镜像 `campus-badminton-app:20260921-fresh-zero1`（SHA-256 `6bdf43c4b0dc1ffddd64dd8986904dd65922ee0a221e55e4b0a89ad12b0c24ad`）healthy；发布包、发布目录、运行文件系统和镜像环境变量均无 `.env*`。
