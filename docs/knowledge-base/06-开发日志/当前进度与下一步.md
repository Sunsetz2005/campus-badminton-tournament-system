---
title: 当前进度与下一步
stage: 4
status: current
updated: 2026-09-24
tags:
  - 羽毛球赛事管理系统
  - 当前进度
---

# 当前进度与下一步

## 当前事实

- 阶段 0 的需求、架构、规则、交互、测试和安全同步文档已完成。
- 阶段 1 的 Next.js/React/TypeScript 工程、PostgreSQL 迁移、Better Auth 登录、服务端角色/指派校验、公开白名单和最小控制会话基础已完成。
- Claude 独立复验判定阶段 1 必需验收通过；后续 S1-010—S1-014 也已全部复验通过。
- 阶段 2 已实现纯 TypeScript 规则引擎：三个演示 profile、抛币、单双打逐分、双打 R/L、发接发、间歇、物理换边、撤销、更正、暂停、特殊结果、提交/确认/重开和确定性重放。
- 规则引擎不读取数据库、网络、当前时间或随机数；命令时间和抛币事实从外部输入。屏幕方向不进入比赛状态。
- Claude 阶段 2 第二轮独立复验判定 S2-002、S2-003、S2-006 通过，S2-001 的替换值校验通过但命令入口阶段门禁缺失，遗留部分另立 S2-007。
- S2-007 第 3 轮独立复验通过：四类更正的结果锁、比分更正 phase 白名单和 `REOPEN_RESULT` 受控重开通路同时成立。
- S2-008 已在 `CORRECT_PHYSICAL_ENDS` 中新增 `obligationRelationship`：`FULFILL` 表示现场已换边并消费待办，`KEEP_PENDING` 只更正原端位记录并保留换边待办；2026-09-20 独立复验通过。
- S2-009 已新增 `INVALIDATE_SPECIAL_OUTCOME`：要求原因，从录入事件的前状态精确恢复，保留作废引用并可确定性重放；2026-09-20 独立复验以 4 种前状态 × 5 类结果通过。
- S2-004/S2-005 是非阻塞遗留。
- 阶段 3 已建立 `MatchSnapshot` 权威快照、只追加命令事件、幂等结果、版本检查、服务端租约心跳和裁判长原子接管。
- 裁判工作台已重构为权威状态驱动的场地图：单双打站位与发接发标记由引擎状态投影，前端不复制计分或换位算法。
- 工作台支持拟首发/拟首接、双打手动换位、物理换边、本机翻转视角和减分预览；翻转视角不产生服务端事件，减分不做算术减分。
- 单一在途锁、非乐观比分和 `UNKNOWN` 原 `commandId` 对账继续成立；离线、失去控制或响应未知时界面保留最后一份已确认状态。
- 合并 UI-01-B 与裁判工作台后的本轮结果：`pnpm test` 101/101，Playwright 30/30，lint/typecheck/build 通过，生产启动安全 4/4，知识库同步脚本测试 10/10；联合独立验收已复跑并确认这些数字一致，但因证据记账 F-001 给 UI-01-B 与联合总判“不通过”。
- 联合独立验收对阶段 3 场地图工作台给出“有条件通过”，未发现功能性 P0—P2；F-001—F-005 已由 Codex 做最小修复，最新本地结果为规则命令定向 64/64、`pnpm test` 101/101、Playwright 36/36（8＋6＋22）、无根 `.env` 的干净副本 build 与生产启动安全 4/4、lint/typecheck、同步脚本 10/10 及阶段 3/UI-01 图片哈希 6/6、45/45 通过，尚待独立复判。
- 全量 Playwright 首跑在共享 `_test` 库残留 `controlGeneration=2` 时为 35/36；阶段 1 夹具原先只删控制会话、未复位代次。补齐测试夹具复位后，在同一残留库上定向 1/1、全量 36/36 通过；这提高测试确定性，不改变产品会话逻辑，也不消除外部并发写入风险。
- Vault 发布前 dry-run 为 `create=4 update=13 unchanged=22 stale=0 conflict=0`，正式同步后复跑为 `create=0 update=0 unchanged=39 stale=0 conflict=0`；仓库/Vault 39/39 份 Markdown 的 SHA-256 一致。该记录是当时发布证据，不代表修复后已独立复判。
- UI-01-A 已完成指定亚运会结果站的本机浏览器核验、现有 `/public` 审计、两份公开端界面规范和 16 张分类证据；附件概念图没有冒充参考站实拍。
- 现有正式 `/public` 仍固定读取 `phase-1-demo`；UI-01-B 已在 `/public/preview/...` 完成每日赛程与比赛详情的隔离模拟预览，不读取数据库或受保护 API。按用户人工测验授权，`/board` 只读取现有公开白名单投影，展示模拟赛事的状态与结果；这只是只读演示看板，不包含 UI-01-C 的正式发布策略、排名或成绩册。
- 用户暂停全站美化后，每日赛程已按单一附图的桌面/手机日程区域完成三轮视觉修正，并根据用户实际读图反馈做一次定向收口：桌面 8 行均为 76px、A/B 向中线收拢、比分扁平化，手机移除独占状态页尾且普通卡为 173px。定向 Vitest 7/7、预览 Playwright 24/24、lint/typecheck/build 通过，1440×1000、390×844、820×1180 与长姓名新基线均实际读取。页面仍待用户确认，未扩展到其他公开页。
- 每日赛程本轮知识库已安全发布：发布前 `update=9 unchanged=31 stale=0 conflict=0`，发布后 `unchanged=40 stale=0 conflict=0`；图片按边界只保留在仓库。
- UI-01-B 文档已安全发布到 Vault：发布前 `create=1 update=9 unchanged=30 stale=0 conflict=0`，发布后 `unchanged=40 stale=0 conflict=0`；截图与生成参考仅保留仓库。
- 联合验收发现收口文档已安全发布：首轮 `update=9 unchanged=31`、备份 `2026-09-20T07-38-38-591Z`；补记发布证据后第二轮更新 3 份、备份 `2026-09-20T07-39-55-262Z`；两轮均 `stale=0 conflict=0`，最终 dry-run 为 `unchanged=40`。记录本句的最终发布只以发布后 dry-run 闭环，不递归写回其备份路径。
- 开发库 `badminton_tournament_dev` 与测试库 `badminton_tournament_test` 继续分离；自动化数据库测试只连接 `_test` 库。
- 2026-09-21 按用户授权完成公网 HTTP 演示部署：目标为公网演示服务器，独立部署目录、独立 Compose 项目/网络/PostgreSQL 15 数据卷，只暴露 TCP 80；同机既有项目的容器与端口均保持健康。应用和数据库容器健康且使用 `unless-stopped`，公网健康检查、首页、三类模拟身份的登录/会话/对应页面和应用重启均实测通过。
- 演示库现含 3 个登录身份、16 名匿名选手、1 个赛事、2 个项目、17 场比赛与 55 条真实计分事件；其中 11 场待开赛、1 场结束待提交、1 场已提交待复核、4 场结果已锁定，含正常比分与 `RET`。四场 `*-FRESH-*` 经生产 SQL 核对均为 `READY/version=0/events=0/0:0/activeOfficial=1`，并在裁判页优先展示。参赛选手身份当前无赛事角色、未与 `Participant` 绑定，只能作为已认证只读身份验证；这不是报名或个人赛程功能。
- 已生成 root-only 初始数据库快照 `<部署目录>/backups/initial-after-seed-20260921.sql.gz`、本次扩充前快照 `<部署目录>/backups/pre-manual-board-20260921.sql.gz` 和扩充后快照 `<部署目录>/backups/manual-board-after-20260921.sql.gz`，三者 gzip 完整性与 SHA-256 通过；没有做恢复演练。部署仍为明文 HTTP 直连 Next.js，未配置 HTTPS、域名或反向代理，因此不是正式生产验收。
- 人工直采模拟数据与只读看板收口后，本地 lint、typecheck、build、全量 Vitest 20 文件 183/183、全量 Playwright 54/54、生产启动安全 4/4 通过；1440×1000 与 390×844 看板浏览器实拍均无根级横向溢出。公网 `/board`、公开 API、三身份登录和对应页面均实际返回 200，同机既有项目保持 200/healthy。
- 最终运行镜像为 `campus-badminton-app:20260921-fresh-zero1`（SHA-256 `6bdf43c4…c24ad`）。首次 board1 打包误带本机 `.env.seed`，但未加载且其两套口令与服务器口令均不同；board2 及后续镜像均使用双重排除，旧镜像与旧包已定向删除。为不影响同机其他项目，没有执行全局 Docker prune。
- 2026-09-20 完成裁判工作台场地化改造（起始 HEAD `84a1592`，起始工作区干净）：
  - 先按需求包 `acceptance/双打验收轨迹.json` 补测试再动实现。**九步双打轨迹、单打 4:3 强制覆盖、单打非每分必换反例、两个手动更正场景全部一次通过**，证实既有引擎与投影本就正确，本轮**没有**修复计分或换位缺陷。
  - 实际修复的是需求符合性缺口 REF-001—REF-007：长姓名三处截断、无真实场线、队名条挤占场地、缺独立大比分行、按钮文案未按画面侧命名、次要操作未折叠、`/officiating` 首页文案过期。
  - 新增 `src/ui/court-geometry.ts` 按 Diagram A 画线（中线只到前发球线），重写 `court-console.tsx`，投影新增 `displayedLeftSide/displayedRightSide`，次要操作收进 `<details>` 抽屉且待提交/复核时默认展开。
  - 本轮结果：lint/typecheck/build 通过，`pnpm test` 114/114（原 101），Playwright 43/43（原 36），生产启动安全 4/4，同步脚本 10/10。
  - 截图 before/after 各 13 张（`artifacts/referee-court-v2/`），覆盖 390×844、820×1180、1180×820、1440×1000、320×720 与长姓名、草稿态、只读态；**before 已回到干净 HEAD 用同一脚本重拍**，两组取证方法一致。
  - 截图中「跳到主要内容」压住场地经实测为 Playwright `fullPage` 对 `position: fixed` 的重排假象（元素实际在 y=-55 且未获焦点），**未改产品样式**，只在截图脚本内临时隐藏。
  - 本轮无数据库迁移、未改 `prisma/seed.ts`（长姓名只在 `_test` 库内临时改名并复原）、未提交、未推送、未部署。
  - 仍未验证：真机手机/平板与真人裁判执裁；BWF PDF 原文未在本机重新比对；三个 profile 仍是演示配置。改造视觉是否满意待用户确认，功能可否正式执裁待组织者与裁判员试用。

- 2026-09-21 执行外部源码审查包 V3（起始 HEAD `70fff5e`，比审查基线 `84a1592` 新一个提交）：
  - 先比较差异，R3-001—R3-005 五项在本机全部仍然成立；没有重复修已解决的问题，也没有回退代码。
  - R3-001 阈值间歇与决胜局换边改为按本局记录一次性触发，阈值全部来自冻结规则快照；引擎升到 v2，**历史事件不改写、不删除**，重放按每条事件记录的引擎形状比对，快照在下一次写入时自然升级。
  - R3-002 界面快照采用改为统一的单调版本策略；旧回执仍然核销待确认命令，但不再把界面写回旧版本。浏览器注入实测：修复前比分从 2 倒退为 1，修复后 30 次采样全为 2。
  - R3-003 区分「4xx 且带结构化错误码的明确拒绝」与「结果未知」；5xx、超时、断网、畸形 200 一律保留原 commandId，不自动生成新 ID 补发同一分；本机存储损坏进入受控恢复而不是清空后假成功。
  - R3-004 `endedAt` 只在跨越「未结束 → 已结束」时写入；10:00 结束、10:05 提交、10:10 复核后仍为 10:00。
  - R3-005 进入可写界面前检测安全随机源；局域网明文 HTTP 使用 `crypto.getRandomValues` 兼容路径并常驻 HTTPS 提示，缺能力时只读。**未做真机验证，只修 UUID 不等于 HTTPS 与登录部署问题已解决。**
  - 性能追加核验后对轮询路径做最小优化：`unchanged` 由 66.8 ms 降到约 2 ms 且不再随事件数增长；行锁、事件重放和哈希校验都没有删除，并有定向回归断言损坏快照仍被发现。
  - 裁判页只做两轮聚焦收敛（局数进比赛栏、压缩重复信息、消除空白控制条），没有新增第二套 UI，也没有重写 `CourtConsole`；只读与异常原因全部保留。
  - 本轮结果：lint/typecheck/build 通过，`pnpm test` 153/153（原 114），Playwright 47/47（原 43），生产启动安全 4/4，同步脚本 10/10。
  - 截图放在独立目录 `artifacts/referee-court-v2/v3-before`、`v3-after` 各 13 张，**上一轮已记账的 `before/`、`after/` 没有被覆盖**。
  - 无 Prisma 迁移，未改 `prisma/seed.ts`，未连接生产数据库，未提交、未推送、未部署。本轮全部是开发自测，不是独立验收。
  - 知识库已安全发布：发布前 dry-run `create=2 update=6 unchanged=37 stale=0 conflict=0`，apply 后为 45 份受管理 Markdown，备份 `2026-09-21T07-24-10-539Z`，发布后复核 dry-run `unchanged=45 stale=0 conflict=0`。本句自身的发布以第二轮发布后 dry-run 闭环，不递归写回其备份路径。截图与量测数据按边界只保留在仓库。

- 2026-09-22 按用户要求完成演示站账号改名与统一演示口令：登录邮箱改为管理员、裁判员、参赛选手三个固定邮箱（见服务器配置），三者公用同一个演示口令（只存在服务器 root-only 配置与交付记录中，知识库不写明文）。
  - 做法是数据库原位更新 `users.email` 与 `accounts` 的三条 credential 摘要，没有新建用户，裁判员的 `OfficialAssignment`/`RoleAssignment` 绑定不变；**没有重跑整份种子**，因为用户已把 `MS-FRESH-001` 手测推进到版本 20 / 20 条事件。
  - 同时修掉一个真实缺陷：`src/components/login-form.tsx` 密码框硬编码 `minLength={12}` 会在前端拦下部分合法口令，已删除该属性。
  - 新镜像 `campus-badminton-app:20260922-account1`（SHA-256 `5337dfca0bfed4257a6dc8b493039be87dc253dfdaabc51563ebd5a3509baafd`），旧镜像与旧 Compose 文件保留可回滚；改名前后快照为 `pre-account-rename-20260922.sql.gz`。
  - 实测：三账号登录与对应页面、`/board`、`/api/health`、同机既有项目均 200；四场 `*-FRESH-*` 在裁判页与 `/board` 可见；本地`pnpm test` 183/183、Playwright 54/54。仍未做恢复演练、HTTPS 与真机验收。
  - 已知行为：Better Auth 对 `/sign-in` 默认限流（10 秒 3 次），连续快速登录会返回 429。

- 2026-09-22 经用户明确授权进入阶段 4，并完成**阶段 4-0 赛事门户**（不含报名与编排）：
  - 迁移 `20260922030854_phase4_tournament_portal` 给 `Tournament` 补门户元数据、新增 `TournamentPhase` 与 `NamePublicationPolicy`、给 `Match` 增加逐场公开发布边界 `publishedAt` 并回填 15 场，四条 CHECK 约束随迁移落库。发布可见性 `TournamentStatus` 未改动，与生命周期 `phase` 分属两个维度。
  - UI-01-C 完成：`src/features/public-preview/` 迁为 `public-results/` 且组件改为 props 驱动 + `mode: preview|live`，新增 `public-projection.ts` 作为 Prisma 行到公开 DTO 的唯一适配器。首页改为赛事列表，新增 `/public/[slug]/schedule` 与 `/matches/[matchCode]`，`/board` 迁到赛事下，原「公开查询」页下线重定向。UI-01-B 隔离夹具及其 24 条用例原样保留。
  - 姓名与代表队由 `Tournament.namePolicy` 决定，默认 `CODES_ONLY` 时真实姓名不离开服务端；赛事时区无效时 fail closed；公开 404 对未知/草稿/未发布/跨赛事统一收敛到同一文案。
  - 导航按服务端会话与角色收敛，未登录不再看到赛事管理、我的执裁和设置。**服务端门禁一行未改**，并新增用例断言直连仍被拒绝。
  - 海报上传按魔数嗅探、2 MiB 上限、服务端重新生成文件名并写审计；数据库 CHECK 兜底非法路径。
  - 本轮结果：lint/typecheck/build 通过，Vitest 22 文件 209/209（原 183），Playwright 71/71（原 54），生产启动安全 4/4，同步脚本 10/10；界面证据 12 张在 `artifacts/phase4-portal/`。
  - 顺带修掉既有记账缺陷：`referee-boundary-ui.spec.ts` 的截图输出改到未提交的 `local-run/`，已提交的 RV3 证据已还原为 HEAD 字节，复跑后无改动。
  - 这只是公开门户。报名、审核、导入、抽签、排名、团体赛和管理端创建向导**全部未实现**。

- 2026-09-24 按用户要求完成**阶段 4-A 赛事创建与报名**（执行者 Claude，详见 [2026-09-24](2026-09-24.md)）：
  - 迁移 `20260924020000_phase4a_registration`：选手按赛事隔离、学号赛事内唯一；新增报名、报名成员、邀请链接、导入批次与平台级 `SYSTEM_ADMIN`；成员数、跨赛事、同项目唯一、状态与 Entry 对应等不变量由数据库兜底。
  - 新建赛事四步向导（项目/组别、演示规则预设、姓名公开策略、按赛事时区的报名窗口、规程说明），阶段推进（筹备→报名中→截止，重开须原因）与门户发布，赛事后台补上海报上传表单。
  - 三种报名来源都只生成「待审核」：后台手工录入、CSV 预览后确认导入（UTF-8/GB18030、行号错误、整份单事务、同一文件只导入一次、原文件不保存）、匿名邀请链接（令牌只存摘要、统一无效提示、双层限流、同意声明、不建账号）。
  - 审核通过才建人并生成 `Entry`；学号是唯一可自动判定同一人的依据，无学号同名必须人工确认；驳回/撤回须原因；改名与换人区分；已开赛或已冻结的项目拒绝变更。
  - 结果：lint/typecheck/build 通过，Vitest 24 文件 243/243，Playwright 75 条中 74 通过（新增 6 条全过；唯一失败为既有 R3-003「502」用例的顺序依赖，HEAD 基线同样失败），生产启动安全 4/4，同步脚本 10/10；证据 10 张在 `artifacts/phase4a-registration/shots/`。
  - 未做：抽签/分组/赛程（4-B/4-C）、开赛推进、.xlsx 直接解析、真机验收、公网演示站迁移与部署；未提交、未推送。全部为开发自测，未独立复判。

## 阶段 2 已完成边界

- [x] `createMatchAggregate`、`applyCommand`、`replayMatch` 均为纯函数式接口，输入失败不修改原聚合。
- [x] 相同 `commandId` 和相同内容返回 `duplicate`；同 ID 不同内容拒绝。
- [x] 传统 21、替代 3×15、单局 21 的延长、封顶、间歇和换边阈值由冻结配置驱动。
- [x] 单打奇偶发球区和双打完整黄金序列逐步断言通过。
- [x] 普通分、阈值分、局末和赛末撤销恢复关联状态；已实际换边时产生物理端核对事项。
- [x] 逻辑 R/L、发接发、物理端和完整比分状态使用不同更正命令并要求原因。
- [x] 事件只追加；重放从初始状态逐条重新执行命令并核对保存快照，篡改快照会拒绝；固定种子随机回合不变量测试通过。
- [x] 完整比分状态更正校验终局、封顶、历史胜局和当前阈值义务；物理端更正在规则换边待办存在时必须显式关联。
- [x] S2-002、S2-003、S2-006 已由 Claude 独立复验通过；S2-001 的替换内容校验部分通过。
- [x] S2-007 已增加结果锁和比分更正 phase 白名单，并由 Claude 独立复验通过。
- [x] S2-008 已用 `obligationRelationship` 显式区分 `FULFILL` 与 `KEEP_PENDING`，并保留旧事件重放兼容入口，独立复验通过。
- [x] S2-009 已增加特殊结果受控作废命令，独立复验覆盖 20 组前状态/结果组合并通过。

## 仍未实现或未验证

- 阶段 4-A 已完成开发自测、未独立复判。阶段 4-B 抽签编排（含抽签发布时设置 `Entry.frozenAt`）、4-C 赛程与裁判排班（含跨项目重叠与休息检查）、4-D 团体赛未开始。
- 4-A 只支持 CSV 导入，不直接解析 .xlsx；按来源限流在无反向代理时可被伪造 `x-forwarded-for` 绕过，真正上限是按邀请的总量与每分钟窗口。
- 公网演示站尚未执行 4-A 迁移；部署前需要迁移并把演示管理员设为 `SYSTEM_ADMIN`，且选手归属回填要求每名选手恰好属于一个赛事。
- 既有 Playwright 用例 `scoring-reliability-injection.spec.ts`「502 时保留原 commandId」与 R3-002 顺序执行时稳定失败（HEAD 基线同样失败），尚未修复；隔离基线库 `badminton_tournament_baseline_test` 保留未删。
- 阶段 4-0 的时间语义只产出 `FIXED` 与 `TBD`；`ESTIMATED/DELAYED/AFTER_MATCH` 需要原计划时间与前序比赛引用列，当前没有，未伪造。
- `projectionRevision` 取响应内 `max(updatedAt)` 的 epoch 毫秒，依赖服务端时钟，不是全局单调序列。
- 公开赛程一次读取该赛事全部已发布比赛再在内存筛选，没有分页。
- 裁判公开显示名未实现；对阵、排名、名次、成绩册仍是「暂未开放」。
- 公网演示站**尚未**为 `public/posters/` 配置数据卷，海报功能上线前必须先补；本轮未做任何生产操作。
- 阶段 4-0 全部是本机开发自测，未独立复判，未做真机与辅助技术验收。

- 真机手机/平板、真人裁判完整执裁、局域网长时运行和系统浏览器旋转为未测试；当前证据是 Chromium 的 375/390 手机竖屏、1024 平板横屏和 200% 字体浏览器模拟。
- 公网 HTTP 演示部署已实测；HTTPS、域名、反向代理、备份恢复演练、正式生产验收和迁移时间戳长期方案仍未验证。
- 三个规则 profile 都是演示配置；尽管场地图实现已按 BWF 条文口径自测，正式赛事仍须老师/组织者正式采纳、确认来源并冻结。
- UI-01-B 与联合总判尚未获得修复后的独立复判；不得把 Codex 本地修复写成验收转通过。
- R3-001—R3-005 的修复同样只有本地开发自测，尚未独立复判。
- 2026-09-21 第二轮按外部复审 RV3 编号完成 RV3-002（抛币完整选择）、RV3-003（暂停中清空待办后恢复）、RV3-004（局末/赛末撤销入口）、RV3-006（更正与普通得分共用推导）；RV3-001/005/007/008 经复核确认已由上一轮修复覆盖，未重复修改。四项新修复同样只有本地开发自测，状态是「已修复待独立复验」。
- 截图证据目录不再被普通测试运行覆盖（两个 e2e spec 默认写入未提交的 `*local-run`）；`artifacts/phase3-browser-simulated/SHA256SUMS` 已按现存字节重算并自校验通过，旧清单保留备查。
- R3-005 的安全上下文兼容只解决命令 ID：HTTPS、反向代理、与之匹配的认证来源、跨设备登录和真机试用仍未做。
- 返回完整快照的读取与每次写入仍是全量重放（100 事件约 67 ms / 74 ms）；并发吞吐与多只读端未量测。
- UI-01-B 已完成本地隔离预览与 Chromium 响应式检查；`/board` 只是现有白名单投影上的演示看板。姓名/成员/代表队/裁判公开策略、逐场发布快照、可信时间和公开投影版本仍是完整 UI-01-C 的前置条件。
- 演示账号名改成固定英文不等于支持裸名登录：项目未启用 Better Auth 的 username 插件，登录页必须输入完整邮箱（裸名会返回 `400 INVALID_EMAIL`）。

## 当前结论：阶段 4-A 已完成开发自测，停在阶段 4-B 之前

阶段 0—3 的结论不变：S2-008、S2-009 已独立复验通过，阶段 3 场地图工作台获「有条件通过」；OFF-011 场地化改造、R3-001—R3-005 与 RV3-002/003/004/006 的修复**都尚未独立复判**，公网 HTTP 演示也不是正式生产验收。

2026-09-22 经用户明确授权进入阶段 4，并完成阶段 4-0 赛事门户：赛事域模型的门户部分、逐场公开发布边界、姓名公开策略、真实数据驱动的每日赛程与比赛详情、导航按角色收敛、海报上传能力。全部本地质量门禁通过，但同样**只是开发自测，未独立复判，视觉也未经用户确认**。

2026-09-24 完成阶段 4-A 赛事创建与报名：向导建赛、后台录入、CSV 导入、匿名邀请链接（不开放公众注册，`disableSignUp` 保持 `true`）、审核通过后生成 `Entry`，到 `Entry` 为止不生成比赛。同样只是开发自测。

下一步是**阶段 4-B：抽签编排**（分组、种子、回避、循环赛/淘汰赛生成、抽签发布冻结报名单位），未经用户明确要求不开始。

仍不把浏览器模拟写成真机验收，不把待采用的 BWF 口径或演示 profile 写成正式规程，不把明文 HTTP 演示写成正式生产验收，也不把公开门户写成完整赛事管理系统。
