---
title: 问题与修复记录
stage: 0
status: current
updated: 2026-09-18
tags:
  - 羽毛球赛事管理系统
  - 问题记录
---

# 问题与修复记录

## 环境与规划问题

| 编号 | 问题 | 影响 | 当前处理 | 状态 |
|---|---|---|---|---|
| ENV-001 | 本机是 Node 26.0.0 Current，没有 Node 24 或版本管理器 | 阶段 1 稳定依赖基线尚未满足 | 阶段 0 不安装；阶段 1 先确认切换方案 | 开放 |
| ENV-002 | Docker/Colima daemon 未运行 | 不能依赖容器启动数据库 | 本机 PostgreSQL 15.18 可复用，因此不阻塞 | 已规避 |
| ENV-003 | Playwright 项目依赖和对应浏览器未安装 | 不能声称端到端环境就绪 | 阶段 1 锁定版本后再安装验证 | 开放 |
| DOC-001 | 提示词包建议路径含 `项目/`，用户明确授权路径不含 | 错选路径会越出本次授权 | 以用户本轮精确路径为唯一目标 | 已决定 |
| RULE-001 | 最新完整竞赛规则未逐条取得和确认 | 不能正式启用计分/排名规则 | 仅建立传统 21 分演示配置和待确认清单 | 开放 |
| TEST-001 | macOS 测试临时目录 `/var` 是系统符号链接 | 首轮同步测试 1 通过、8 失败 | 测试夹具先 `realpath(tmpdir())`，生产路径拒绝策略不放宽；复测 9/9 | 已修复 |

## 记录规则

后续问题按 `模块-序号` 记录发现版本、复现步骤、证据、根因、修复提交和回归结果。只有实际验证成功后才能标记“已修复”；规避措施不能冒充根因修复。
