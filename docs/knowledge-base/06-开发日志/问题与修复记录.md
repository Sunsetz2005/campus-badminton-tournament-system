---
title: 问题与修复记录
stage: 0
status: current
updated: 2026-09-18
tags:
  - 羽毛球赛事管理系统
  - 问题记录
---

# 问题与修复记录

## 环境与规划问题

| 编号 | 问题 | 影响 | 当前处理 | 状态 |
|---|---|---|---|---|
| ENV-001 | 本机是 Node 26.0.0 Current，没有 Node 24 或版本管理器 | Prisma 7.10 明确只支持 Node 20/22/24 | 用 pnpm 项目 runtime 固定 24.21.0，不替换系统 Node；Prisma 实测 Node 24 | 已解决 |
| ENV-002 | Docker/Colima daemon 未运行 | 不能依赖容器启动数据库 | 本机 PostgreSQL 15.18 可复用，因此不阻塞 | 已规避 |
| ENV-003 | Playwright 项目依赖和对应浏览器未安装 | 不能声称端到端环境就绪 | 已锁定 Playwright 1.63.0 并安装 Chromium 153，5/5 通过 | 已解决 |
| DOC-001 | 提示词包建议路径含 `项目/`，用户明确授权路径不含 | 错选路径会越出本次授权 | 以用户本轮精确路径为唯一目标 | 已决定 |
| RULE-001 | 最新完整竞赛规则未逐条取得和确认 | 不能把项目配置宣称为 2026 年现行国际规则 | 已取得 S1/S2/S3 指定版本及用户赛事方案；只在赛事采纳后启用，S3 仍不称为当前完整规则 | 部分解决 |
| TEST-001 | macOS 测试临时目录 `/var` 是系统符号链接 | 首轮同步测试 1 通过、8 失败 | 测试夹具先 `realpath(tmpdir())`，生产路径拒绝策略不放宽；原有用例复测 9/9，本轮扩展后 10/10 | 已修复 |
| SYNC-001 | Vault 中 1 个受管理文件有人工排版改动 | 普通 apply 按策略整体拒绝，不能直接覆盖 | 将目标改动逐字合并回仓库，核对正文 SHA-256 一致后执行严格基线采纳 | 已解决且保留改动 |
| SYNC-002 | 原同步工具没有“正文已合并但清单仍旧”的安全收口命令 | 只能手工改清单或覆盖正文，均不理想 | 新增 `--adopt-target`；内容不一致、有其他变更或非单一人工冲突时全部拒绝 | 已修复并测试 |
| DEP-001 | Vitest 的 Vite peer 首次解析到 `vite@8.3.0`，当时依赖的 PostCSS 版本无法取得 | 首次 `pnpm install` 失败 | 显式固定可解析的 Vite 8.2.1，重装及测试通过 | 已修复 |
| DEP-002 | ESLint 10 与 Next 间接的 React/import/a11y 插件 peer 范围不兼容；ESLint 9 包显示维护期警告 | 选 ESLint 10 会保留 peer 冲突，选 9 会有弃用提醒 | 暂锁 9.39.5 以保持 peer 零冲突和 lint 通过；待 Next 插件链支持 10 后升级 | 开放升级项 |
| TEST-002 | 首次刷新用例错误期待公开页显示内部比赛代码 | 1 条失败，串行后续 4 条未运行 | 改为验证刷新后公开对阵仍存在，完整重跑 5/5 | 已修复 |

## 记录规则

后续问题按 `模块-序号` 记录发现版本、复现步骤、证据、根因、修复提交和回归结果。只有实际验证成功后才能标记“已修复”；规避措施不能冒充根因修复。
