---
title: 问题与修复记录
stage: 0
status: current
updated: 2026-09-20
tags:
  - 羽毛球赛事管理系统
  - 问题记录
---

# 问题与修复记录

## 环境与规划问题

| 编号 | 问题 | 影响 | 当前处理 | 状态 |
|---|---|---|---|---|
| ENV-001 | 本机是 Node 26.0.0 Current，没有 Node 24 或版本管理器 | Prisma 7.10 明确只支持 Node 20/22/24 | 用 pnpm 项目 runtime 固定 24.21.0，不替换系统 Node；Prisma 实测 Node 24 | 已解决 |
| ENV-002 | Docker/Colima daemon 未运行 | 不能依赖容器启动数据库 | 本机 PostgreSQL 15.18 可复用，因此不阻塞 | 已规避 |
| ENV-003 | Playwright 项目依赖和对应浏览器未安装 | 不能声称端到端环境就绪 | 已锁定 Playwright 1.63.0 并安装 Chromium 153，5/5 通过 | 已解决 |
| DOC-001 | 提示词包建议路径含 `项目/`，用户明确授权路径不含 | 错选路径会越出本次授权 | 以用户本轮精确路径为唯一目标 | 已决定 |
| RULE-001 | 最新完整竞赛规则未逐条取得和确认 | 不能把项目配置宣称为 2026 年现行国际规则 | 已取得 S1/S2/S3 指定版本及用户赛事方案；只在赛事采纳后启用，S3 仍不称为当前完整规则 | 部分解决 |
| TEST-001 | macOS 测试临时目录 `/var` 是系统符号链接 | 首轮同步测试 1 通过、8 失败 | 测试夹具先 `realpath(tmpdir())`，生产路径拒绝策略不放宽；原有用例复测 9/9，本轮扩展后 10/10 | 已修复 |
| SYNC-001 | Vault 中 1 个受管理文件有人工排版改动 | 普通 apply 按策略整体拒绝，不能直接覆盖 | 将目标改动逐字合并回仓库，核对正文 SHA-256 一致后执行严格基线采纳 | 已解决且保留改动 |
| SYNC-002 | 原同步工具没有“正文已合并但清单仍旧”的安全收口命令 | 只能手工改清单或覆盖正文，均不理想 | 新增 `--adopt-target`；内容不一致、有其他变更或非单一人工冲突时全部拒绝 | 已修复并测试 |
| DEP-001 | Vitest 的 Vite peer 首次解析到 `vite@8.3.0`，当时依赖的 PostCSS 版本无法取得 | 首次 `pnpm install` 失败 | 显式固定可解析的 Vite 8.2.1，重装及测试通过 | 已修复 |
| DEP-002 | ESLint 10 与 Next 间接的 React/import/a11y 插件 peer 范围不兼容；ESLint 9 包显示维护期警告 | 选 ESLint 10 会保留 peer 冲突，选 9 会有弃用提醒 | 暂锁 9.39.5 以保持 peer 零冲突和 lint 通过；待 Next 插件链支持 10 后升级 | 开放升级项 |
| TEST-002 | 首次刷新用例错误期待公开页显示内部比赛代码 | 1 条失败，串行后续 4 条未运行 | 改为验证刷新后公开对阵仍存在，完整重跑 5/5 | 已修复 |

## 阶段 1 Claude 审查问题

### S1-001｜公开注册与模拟种子开关耦合

- 根因复核：成立。`ALLOW_DEMO_ACCOUNTS` 同时打开种子和公众注册，Playwright 又覆盖了真实测试配置。
- 修改文件：`src/server/auth/auth.ts`、`prisma/seed.ts`、`playwright.config.ts`、`tests/e2e/phase-1.spec.ts`。
- 回归用例与结果：修复前真实配置注册用例得到 200 并失败；修复后 Playwright 8/8，全新 `_test` 库两次种子均成功。
- 剩余限制：真实生产部署未运行。
- 状态：**已修复，待复验**。

### S1-002｜页面权限错误变成 HTTP 500

- 根因复核：成立。Server Component 抛 `AppError` 被 Next 转为 500。
- 修改文件：`next.config.ts`、`src/server/auth/page-authorization.ts`、`src/app/forbidden.tsx`、`src/app/management/forbidden.tsx`、`src/app/public/not-found.tsx`、三个受保护页面与公开页。
- 回归用例与结果：修复前裁判访问管理页实测 500；修复后管理页 403 和公开空态 404 用例通过。
- 剩余限制：`authInterrupts` 在 Next 16.3.5 仍标记实验功能，升级 Next 时需复核。
- 状态：**已修复，待复验**。

### S1-003｜健康检查服务端时间偏移八小时

- 根因复核：成立。Prisma PG adapter 将本地墙钟的 `timestamptz` 解析为 UTC `Date`。
- 修改文件：`src/app/api/health/route.ts`、`tests/e2e/phase-1.spec.ts`。
- 回归用例与结果：修复前对时回归显示约 +8 小时并失败；修复后返回时间落在请求前后 5 秒容差内。
- 剩余限制：旧迁移的 `timestamp without time zone` 默认值是长期风险；当前无数据损坏证据，本轮未改 schema/迁移。
- 状态：**已修复，待复验**。

### S1-004｜干净检出缺少测试环境模板

- 根因复核：成立。没有可跟踪的 `.env.test` 模板，缺失配置时只有泛化错误。
- 修改文件：`.env.test.example`、`.gitignore`、`src/db/database-safety.ts`、`tests/db/database-safety.test.ts`、`tests/server/test-environment-template.test.ts`、`README.md`、本地启动文档。
- 回归用例与结果：修复前模板不存在、只报“缺少 DATABASE_URL”；修复后对应测试通过，`pnpm test` 16/16。
- 剩余限制：首次使用者仍需按本机 PostgreSQL 填写不提交的值。
- 状态：**已修复，待复验**。

### S1-005｜拒绝审计缺失且脱敏日志未接入

- 根因复核：成立，但原建议过宽；所有 401/404 都写业务审计会制造无效流量。本轮让通用 API 失败进入结构化脱敏服务日志，只对已登录用户的控制权 403 写 `DENIED` 审计。
- 修改文件：`src/server/services/errors.ts`、两个 API route、`src/server/services/scoring-session-service.ts`、`tests/server/errors.test.ts`、`tests/integration/authorization.test.ts`。
- 回归用例与结果：修复前日志 spy 为 0 调用、拒绝审计为 null；修复后敏感键为 `[REDACTED]`，403 审计包含 `not_assigned`，控制会话计数不变。
- 剩余限制：未知 500 只记 code/路由等脱敏元数据，不记堆栈与原始请求。
- 状态：**已修复，待复验**。

### S1-006｜e2e 残留活动控制会话

- 根因复核：成立。原 e2e 只在用例前删除控制会话。
- 修改文件：`tests/e2e/phase-1.spec.ts`、`tests/integration/authorization.test.ts`。
- 回归用例与结果：新增 `afterEach` 清理和 403 无业务状态变化断言；全量 e2e 后 `scoringSessions=0`、相关 `auditLogs=0`、`sessions=0`、`blockedUsers=0`。
- 剩余限制：清理只针对隔离测试库的本套件产物。
- 状态：**已修复，待复验**。

### S1-007｜Vault 路径疑义

- 根因复核：实现缺陷不成立。用户已确认无 `项目/` 层级的路径，磁盘、代码和授权一致。
- 修改文件：`docs/knowledge-base/02-架构设计/技术选型与决策.md`，只补 ADR 避免重复误判。
- 回归用例与结果：同步脚本测试 10/10；真实目标 dry-run 结果见测试记录。
- 剩余限制：不改写仓库外 `prompt-kit/01`。
- 状态：**非实现缺陷；文档已补，待复验**。

### S1-008｜裁判长接管角色前置风险

- 根因复核：阶段 1 当前不构成可触发缺陷；原报告也定义为阶段 3 前置风险。
- 修改文件：`docs/knowledge-base/02-架构设计/API与权限.md`；明确当前 `requireAssignedReferee` 会拒绝 `CHIEF_REFEREE`，矩阵是阶段 3 目标而非现状。
- 回归用例与结果：`tests/server/project-guidance.test.ts` 检索 `CHIEF_REFEREE` 当前限制通过；现有阶段 1 权限回归通过。
- 剩余限制：阶段 3 实现接管时必须新增裁判长授权用例。
- 状态：**阶段 3 前置风险；文档已修复，待复验**。

### S1-009｜安装命令与权限门禁文档失准

- 根因复核：成立。历史安装命令与当前指引没有区分，权限门禁误包阶段 2/3 项。
- 修改文件：`docs/knowledge-base/05-测试验收/测试记录.md`、`docs/knowledge-base/02-架构设计/API与权限.md`、`README.md`。
- 回归用例与结果：`pnpm install --frozen-lockfile` 实际 0 退出；文档不再把过期版本/无效租约写成阶段 1 已覆盖。历史 `--frozen-lockfile=false` 记录保留为当时事实。
- 剩余限制：历史命令不为“看起来一致”而篡改。
- 状态：**已修复，待复验**。

## 阶段 1 Claude 独立复验后续问题

### S1-010｜生产安全门禁未在服务启动前终止进程

- 根因复核：成立。Next 16.3.5 会编译并注册 `instrumentation`，但 `next start` 在请求处理器完成准备前先监听并打印 Ready；只在认证模块加载时检查只能让鉴权请求失败。
- 修改文件：`src/instrumentation.ts`、`scripts/start-production-server.ts`、`package.json`、`tests/server/production-startup-security.test.mjs`、`CLAUDE.md`。
- 回归用例与结果：修复前非法配置下进程进入 Ready；修复后项目生产启动命令在调用真实 `next start` 前失败，非法/合法配置进程测试 2/2，合法配置健康检查 200。
- 实际命令：`pnpm run test:production-startup-security`；另以已有构建运行 `dotenv -e .env.test -- node --test tests/server/production-startup-security.test.mjs` 得到 2/2。
- 剩余限制：真实部署平台、容器编排和外部健康探针仍未验证；直接绕过项目脚本调用 Next CLI 不属于受支持启动方式。
- 状态：**已修复，待复验**。

### S1-011｜裁判长接管限制的文档留痕缺失

- 根因复核：成立。原修复记录声称限制已在 `API与权限.md` 中，但该文档没有 `CHIEF_REFEREE` 当前实现说明。
- 修改文件：`docs/knowledge-base/02-架构设计/API与权限.md`、本文件、`tests/server/project-guidance.test.ts`。
- 回归用例与结果：修复前文档一致性测试失败；修复后明确区分当前授权和阶段 3 目标，定向测试通过。
- 实际命令：`dotenv -e .env.test -- vitest run tests/server/project-guidance.test.ts`（纳入本轮定向 9/9）。
- 剩余限制：阶段 3 仍须真正实现并测试裁判长接管。
- 状态：**已修复，待复验**。

### S1-012｜重复种子不更新既有身份口令

- 根因复核：成立。`ensureAuthUser` 只在用户不存在时建立密码凭据，既有用户仅更新显示字段。
- 修改文件：`prisma/seed.ts`、`tests/integration/seed-auth.test.ts`。
- 回归用例与结果：修复前轮换测试中新口令校验为 false；修复后只在哈希不匹配时更新凭据，新口令通过、旧口令失败、凭据计数不变，测试结束恢复原测试口令。
- 实际命令：隔离库定向 Vitest 9/9；全量 `pnpm test` 20/20。
- 剩余限制：只处理本地模拟身份；生产用户口令不通过种子管理。
- 状态：**已修复，待复验**。

### S1-013｜拒绝审计可被重复请求放大

- 根因复核：成立。每次相同 403 都无条件创建一行审计，并发请求会同时通过。
- 修改文件：`src/server/services/scoring-session-service.ts`、`tests/integration/authorization.test.ts`。
- 回归用例与结果：修复前 8 个并发请求写入 8 条；修复后按操作者、比赛、错误码和 60 秒窗口生成确定性 UUID，以数据库唯一主键并发抑制为 1 条。
- 实际命令：隔离库定向 Vitest 9/9；全量 `pnpm test` 20/20。
- 剩余限制：这是业务审计写入抑制，不替代阶段 3 的接口级限流、网关防护或跨错误码总量限制。
- 状态：**已修复，待复验**。

### S1-014｜`CLAUDE.md` 保留已失效工程事实

- 根因复核：成立。测试环境模板、公众注册耦合和知识库固定数量三处陈述均已失准。
- 修改文件：`CLAUDE.md`、`tests/server/project-guidance.test.ts`。
- 回归用例与结果：修复前两条文档一致性测试均失败；修复后模板、开关职责和动态文件清单说明与当前实现一致，测试通过。
- 实际命令：定向 Vitest 9/9；全量 `pnpm test` 20/20。
- 剩余限制：`CLAUDE.md` 不发布到 Vault，后续实现变化仍须同步维护。
- 状态：**已修复，待复验**。

## 阶段 2 Claude 独立审查问题

### S2-001｜完整比分更正可写入自相矛盾状态

- 根因复核：成立。`applyReplacement` 只校验非负整数、成员和发接发位置，没有校验局分是否已经结束、已完成局与胜局数、封顶分及当前阈值义务。
- 修改文件：`src/domain/rules/match-engine.ts`、`tests/domain/match-engine.test.ts`、规则说明与阶段状态文档。
- 回归用例与结果：新增“已结束局仍标记进行中”“胜局与已完成局不一致”“单局 21 在 11 分缺少间歇/换边义务”三个拒绝断言，并验证显式提交完整阈值义务可接受。修复前用例失败；修复后阶段 2 定向 26/26、全量 Vitest 47/47。
- 实际命令：`pnpm exec vitest run tests/domain/match-engine.test.ts`（修复前 3 条新增用例失败，修复后 26/26）；`pnpm test`（47/47）。
- 剩余限制：阶段 3 的裁判长审批、影响预览和数据库事务尚未实现；三个 profile 仍是演示配置。
- 状态：**替换内容校验已独立复验通过；入口阶段门禁遗留见 S2-007**。

### S2-002｜物理端更正未关联规则换边义务导致二次交换

- 根因复核：成立。`CORRECT_PHYSICAL_ENDS.fulfillsObligationId` 只接受 `PHYSICAL_ENDS_REVIEW`，存在 `CHANGE_ENDS` 时仍允许裸更正。
- 修改文件：`src/domain/rules/match-engine.ts`、`tests/domain/match-engine.test.ts`、`03-竞赛规则/换边撤销与更正.md`。
- 回归用例与结果：存在规则换边义务时裸更正被拒且原聚合不变；显式引用 `CHANGE_ENDS` 后以提交端位作为现场最终事实、消费义务且不再次交换。修复前裸更正错误地被接受；修复后定向和全量测试通过。
- 实际命令：`pnpm exec vitest run tests/domain/match-engine.test.ts`（26/26）；`pnpm test`（47/47）。
- 剩余限制：阶段 3 UI 必须向裁判明确展示“同时完成规则换边”的选择，不能自动替用户推断现场事实。
- 状态：**独立复验通过**。

### S2-003｜事件重放直接采用保存快照

- 根因复核：成立。原 `replayMatch` 只检查版本连续，然后把 `event.stateAfter` 赋给当前状态；篡改为 99:99 会被原样返回。
- 修改文件：`src/domain/rules/match-engine.ts`、`tests/domain/match-engine.test.ts`、`03-竞赛规则/计分与双打发接发.md`。
- 回归用例与结果：重放改为从初始状态逐条重新执行事件命令，并核对重新生成的完整事件；篡改 `stateAfter` 后抛出快照不一致，正常 JSON 往返和包含撤销事件的重放一致。修复前篡改用例失败；修复后定向 26/26、全量 47/47。
- 实际命令：`pnpm exec vitest run tests/domain/match-engine.test.ts`；`pnpm run typecheck`；`pnpm run build`。
- 剩余限制：阶段 3 尚未把事件写入数据库，也未实现事件签名或外部防篡改存储；当前保证是事件内部命令与快照一致性。
- 状态：**独立复验通过**。

### S2-006｜知识库文档数量硬编码失准

- 根因复核：成立。追踪矩阵和里程碑把会随审查报告、日期日志变化的 Markdown 数量写成固定值。
- 修改文件：`01-项目规划/需求验收追踪矩阵.md`、`01-项目规划/里程碑与任务看板.md`。
- 回归用例与结果：改为由同步清单动态核对，不再维护固定数量；同步测试和真实 Vault dry-run/apply 结果见测试记录。
- 剩余限制：S2-004 与 S2-005 本轮未修，按独立审查结论不阻塞阶段 3。
- 状态：**独立复验通过**。

### S2-007｜更正命令缺少结果锁和比分更正阶段门禁

- 根因复核：成立。四个 `CORRECT_*` 分支没有统一的 `SUBMITTED`/`CONFIRMED` 结果锁；`CORRECT_SCORE_STATE` 只校验替换值内部一致性，不校验命令发出时的聚合 phase。
- 修改文件：`src/domain/rules/match-engine.ts`、`tests/domain/match-engine.test.ts`、规则说明、测试记录和阶段状态文档。
- 回归用例与结果：已提交状态的比分更正、已确认状态的四类更正均返回 `result_locked` 且聚合不变；未抛币、特殊结果和暂停状态的比分更正返回 `score_correction_not_allowed`；`GAME_COMPLETE` 和 `REOPEN_RESULT` 后的 `MATCH_COMPLETE_PENDING_SUBMISSION` 仍允许合法比分更正。
- 实际命令：新增回归修复前 2 条失败；修复后 `pnpm exec vitest run tests/domain/match-engine.test.ts` 28/28，`pnpm test` 49/49，lint/typecheck/build、Playwright 8/8、生产启动 2/2 均通过。
- 剩余限制：阶段 3 仍须在服务端授权、`ResultRevision` 和数据库事务中执行同一门禁；纯引擎不承担申请人、批准人和影响预览持久化。
- 状态：**第 3 轮独立复验通过**。

### S2-008｜规则换边待办存在时无法表达“端位更正与换边无关”

- 根因复核：成立。原载荷只能引用并消费 `CHANGE_ENDS`，无法如实表达“只更正原端位记录，现场换边仍待执行”。
- 修复：`CORRECT_PHYSICAL_ENDS` 新增 `obligationRelationship: { obligationId, action }`；`FULFILL` 消费待办，`KEEP_PENDING` 保留待办。旧 `fulfillsObligationId` 只作为历史事件重放兼容入口，新旧字段并用拒绝。
- 门禁：错误待办 ID、无待办伪造关联、特殊结果待提交、已提交/已锁定和其他非白名单 phase 均拒绝。
- 回归证据：纯引擎覆盖 `FULFILL`、`KEEP_PENDING`、错误 ID、幂等/异载荷和旧事件 JSON 重放；测试库 HTTP/事务路径验证待办、事件和快照同一事务更新。本轮定向 64/64、全量 Vitest 94/94 通过。
- 剩余限制：尚未获得 Claude 独立复验或真人裁判现场验收。
- 状态：**已实现并通过本地规则/HTTP 事务回归，待独立复验**。

### S2-009｜特殊结果记录缺少受控作废通路

- 来源：Claude 第 3 轮独立复验新发现，P3，不阻塞阶段 3。
- 根因复核：成立。`RECORD_SPECIAL_OUTCOME` 会进入待提交状态，原命令集无法恢复记录前的比分、发接发和 phase。
- 修复：新增 `INVALIDATE_SPECIAL_OUTCOME`，只在 `SPECIAL_OUTCOME_PENDING_SUBMISSION` 下接受，要求原因，并根据对应录入事件的 `stateBefore` 精确恢复；作废事件保存 `invalidatedCommandId`。
- 额外门禁：特殊结果待提交时不允许直接覆盖；已提交/确认时直接作废拒绝，受控重开后可作废。
- 本地证据：定向 31/31，全量 Vitest 52/52，Playwright 8/8，lint/typecheck/build 和生产启动门禁 2/2 通过。
- 状态：**已实现并通过本地回归，待独立复验**。

## 阶段 3 场地图工作台本地收口

### OFF-COURT-001｜比分优先界面不足以表达规则站位

- 问题：原工作台主要展示双方比分卡和文本发接发状态，裁判不能一眼核对双打四人左右发球区、当前物理半场和应发球人。
- 修复：新增纯投影 `buildCourtViewModel(state, flipped)` 和四格球场工作台；前端只投影权威 `logicalCourts`、`physicalEnds`、`serverPlayerId`、`receiverPlayerId`，不复制计分/换位算法。内联 SVG 羽毛球和文字共同标识发球员。
- 回归证据：END_1/END_2 × 正常/翻转 × R/L 矩阵、单打 0:0/1:0/1:1/1:2、双打黄金序列、自动换位与物理换边均通过。换边只移动队伍和其比分面板，`score.A/B` 归属不变。
- 状态：**已实现并通过本地回归，待独立复验、真机与真人裁判验收**。

### OFF-COURT-002｜默认名单第一人不能代替真实首发/首接选择

- 问题：双打首局/下一局若固定名单第一人，会把名单顺序误当成现场首发与首接决定。
- 修复：开局设置显式选择首发球员和首接球员，由服务端权威状态生成双方合法 R/L。
- 回归证据：Playwright 选择 A/B 各自非名单第一人，开局、刷新后核对发接发与四格位置一致。
- 状态：**已修复并通过本地浏览器回归，待现场验收**。

### OFF-COURT-003｜“−”及响应未知可造成虚假成功感

- 问题：若“−”直接算术减分，或在服务器响应未知时更改本地正式比分，都会产生与权威事件不一致的展示。
- 修复：“−”只打开撤销预览，必须填原因并核对最近一分归属；工作台不乐观改分。响应未知时只读，保留完整原命令并用原 `commandId` 对账。
- 回归证据：撤销预览和取消前后比分、版本和事件数不变；响应中止后查回已提交结果，对应事件只有一条。
- 状态：**已修复并通过本地浏览器/数据库回归，真机网络切换未测试**。

## 记录规则

后续问题按 `模块-序号` 记录发现版本、复现步骤、证据、根因、修复提交和回归结果。只有实际验证成功后才能标记“已修复”；规避措施不能冒充根因修复。
