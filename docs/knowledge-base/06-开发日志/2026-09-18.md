---
title: 2026-09-18 开发日志
stage: 0
status: current
updated: 2026-09-18
tags:
  - 羽毛球赛事管理系统
  - 开发日志
---

# 2026-09-18 开发日志

## 目标

只完成阶段 0：环境检查、需求与架构规划、项目指令、README、知识库初版和安全单向同步，不创建业务应用。

## 已核对事实

- 代码目录：`/Users/Shared/Files From d.localized/校园羽毛球赛事编排与成绩管理系统`，开始时为空且不是 Git 仓库。
- Vault：`/Users/a10954/Documents/Documents - Sunsetz的MacBook Pro/Obsidian Vault`，存在、非符号链接，目标项目目录开始时不存在。
- Node.js 26.0.0、pnpm 11.9.0、Git 2.54.0。
- PostgreSQL 15.18 正在默认端口接受连接。
- Docker 29.7.0 / Compose 5.3.1 客户端存在，但 Colima daemon 未运行。
- Obsidian CLI 可用，App 版本 1.12.7。

## 阶段 0 文件工作

- 创建仓库协作约定、初学者 README、忽略和编辑器规则。
- 创建知识库规划文档，并明确所有未实现/待确认状态。
- 创建无第三方依赖的知识库同步脚本和 `node:test` 安全测试。

## 命令与结果

| 命令/检查 | 实际结果 |
|---|---|
| `node --test tests/sync-knowledge-base.test.mjs` | 第二轮 9/9 通过；首轮 `/var` 符号链接问题已保留记录 |
| 文档 YAML 与相对链接检查 | 26 份文档，失败 0 |
| 真实目标首次 dry-run | 创建 26、冲突 0、未写入 |
| `node scripts/sync-knowledge-base.mjs --apply` | 成功发布 26 个 Markdown |
| 同步后二次 dry-run | 26 个 `UNCHANGED`，其余为 0 |
| 清单与逐文件 SHA-256 | 清单 26，哈希差异 0 |
| Obsidian CLI 读取首页 | 成功返回首页 YAML 和正文 |

本地 Git 基线提交已经创建；最终 SHA 以 Git 历史和交付报告为准，避免在提交内记录会随 amend 改变的自身哈希。

## 阶段 1：工程骨架、数据模型与权限

- 创建 Next.js 16.3.5 / React 19.3.0 / TypeScript 5.9.3 工程，pnpm 锁文件固定全部依赖。
- 没有安装全局版本管理器；用 pnpm `devEngines.runtime` 为项目下载并固定 Node 24.21.0，系统 Node 26 保持不变。
- 创建 Prisma 7.10 schema 和两次 PostgreSQL 迁移，开发库/测试库严格分离。
- 使用 Better Auth 1.7.5 建立数据库会话、密码哈希、登录和关闭公开注册；生产环境发现示例账号配置时拒绝启动。
- 建立赛事角色、裁判指派和单场唯一活动控制会话；没有实现逐分、心跳或接管。
- 创建首页、赛事管理、我的执裁、公开查询、设置和登录页面骨架，所有未实现操作均以文本说明，没有假成功按钮。
- 创建可重复模拟种子：1 场单打、1 场双打、6 名匿名选手、1 个管理员和 1 个临场裁判。

实际验证：lint、typecheck、生产构建通过；Vitest 12/12、Chromium 5/5 通过。首次 Chromium 复测有 1 条错误断言失败、4 条未运行；修正测试后完整重跑 5/5，未把未运行项算作成功。完整命令见[测试记录](../05-测试验收/测试记录.md)。

## 赛事规则 v1.0 补充

- 读取 `/Users/a10954/Downloads/badminton-rules-v1` 中的规则、补充提示词、成绩册模板说明、样例检查记录以及模拟资产清单。
- 将可配置并冻结的规则版本、五单项后台录入/导入、分组晋级、异常结果、抛币、单设备写入、复核锁定、更正版本和公开字段白名单纳入需求与技术设计。
- 新增赛事执行规则正文、需求差异记录、24 条后续验收用例和模板边界说明；没有复制可执行 HTML、模拟 JSON 或 PDF 到 Vault。
- 首次真实目标 dry-run 检出 1 个受管理文件的人工排版改动。先逐字合并回仓库并核对哈希，再使用严格 `--adopt-target` 只更新同步清单基线，未覆盖 Vault 正文。
- 同步工具新增 `--adopt-target` 安全门禁及自动测试；只有仓库与目标逐字一致、没有其他变更且冲突类型单一时才允许采纳。

赛事规则增量同步、30 份文档链接检查和最终哈希复核结果见[测试记录](../05-测试验收/测试记录.md)。

## 阶段 1 Claude 审查问题修复

- 按 S1-001—S1-009 逐项核对；先新增或纠正回归用例，确认注册、页面状态码、UTC 时间、测试环境模板、拒绝审计和测试残留的实际失败。
- 拆开模拟种子授权与网页公众注册；补充 Next 403/404 中文页、数据库 UTC 字符串健康时间、测试环境模板和结构化脱敏日志。
- 已登录用户在控制会话授权中被 403 拒绝时保留 `DENIED` 审计，但不创建控制会话；通用 401/404 仅记结构化服务日志，避免把所有无效流量写入业务审计表。
- S1-007 是过时提示词路径，不是实现缺陷；S1-008 属阶段 3 裁判长接管风险，本轮未改授权范围。
- Codex 回归为 Vitest 16/16、Playwright 8/8、lint/typecheck/build 通过；该结果不写成 Claude 独立复验通过。

## 阶段 1 独立复验后续修复

- Claude 独立复验判定阶段 1 必需验收通过，并新增 S1-010—S1-014 五项非阻塞问题。
- 为生产启动门禁增加进程级测试和启动前包装器；`instrumentation` 继续作为请求准备期纵深检查。
- 种子现在可安全轮换既有模拟身份口令；同一拒绝原因在 60 秒窗口内以数据库唯一键抑制重复审计写入。
- 补齐裁判长接管当前限制和 `CLAUDE.md` 三处失准事实；新增文档一致性回归。
- 实际回归为 Vitest 20/20、Playwright 8/8、生产启动 2/2、同步测试 10/10；测试库残留计数均为 0。五项状态均为“已修复，待复验”。

## 阶段 2：计分与双打规则引擎

- 按用户明确授权读取阶段 2 原始提示词、规则来源、裁判端规范、测试计划和当前代码，只实现纯 TypeScript 规则层。
- 新增传统 21、替代 3×15、单局 21 三个演示配置，正式赛事采用状态保持待确认。
- 新增 `createMatchAggregate`、`applyCommand`、`replayMatch`，状态分离 A/B、球员、逻辑 R/L、物理端和屏幕方向；屏幕方向不进入比赛事件。
- 实现抛币、首局/后续局设置、单双打逐分、LET、间歇、规则换边、漏换边、更正、撤销、暂停、特殊结果、提交/确认和受控重开。
- 撤销只处理最近可逆业务命令，不能反撤销；需要重做时发送新命令。已经实际换边的阈值分撤销后生成物理端核对事项。
- 阶段 2 定向测试 26/26、全量 Vitest 44/44、Playwright 8/8、生产启动 2/2，lint/typecheck/build 通过。
- 没有新增 Prisma 迁移、API 或裁判 UI；阶段 3 的数据库事务、多设备和接管仍未实现。本轮完成后停止，等待独立复验。
