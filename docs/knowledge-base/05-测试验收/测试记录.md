---
title: 测试记录
stage: 3
status: current
updated: 2026-09-19
tags:
  - 羽毛球赛事管理系统
  - 测试记录
---

# 测试记录

> 本文件只记录实际执行结果。业务规则和应用验收仍保持“未测试”。

## 阶段 0

| 项目 | 命令 | 结果 | 证据/说明 |
|---|---|---|---|
| 同步工具自动测试 | `node --test tests/sync-knowledge-base.test.mjs` | 10/10 通过 | 覆盖 dry-run、幂等、备份、冲突、无删除、越界、符号链接、中文空格路径和严格目标基线采纳 |
| 文档完整性与链接 | Node 只读检查 | 通过 | 30 份非空文档；YAML/相对链接失败 0 |
| 真实 Vault dry-run | `node scripts/sync-knowledge-base.mjs --dry-run` | 通过 | 首次计划创建 26，冲突 0，目标当时不存在 |
| 真实 Vault apply | `node scripts/sync-knowledge-base.mjs --apply` | 通过 | 发布 26 个受管理 Markdown，未写 Vault 其他目录 |
| 同步后复核 | 再次 dry-run、哈希、Obsidian CLI read | 通过 | 26 个 `UNCHANGED`；清单 26；哈希差异 0；首页读取成功 |
| 人工改动保护与基线采纳 | dry-run、逐字合并、`--adopt-target`、再次 dry-run | 通过 | 检出 1 个受管理文件的人工排版改动；先合并回仓库，正文哈希一致后只更新清单基线，没有覆盖 Vault 正文 |
| 赛事规则 v1.0 增量同步 | dry-run、apply、再次 dry-run、哈希、Obsidian CLI read | 通过 | 初次计划 4 创建、25 更新、1 未变、0 冲突；复核为 30 个 `UNCHANGED`、清单 30、源/目标哈希差异 0，新规则正文读取成功；HTML/PDF/JSON 模拟资产未进入 Vault |

自动测试首轮曾因 macOS `/var` 是 `/private/var` 的系统符号链接而出现 8 个失败。测试夹具改为先解析临时目录真实路径，生产代码继续拒绝同步路径中的符号链接；原有用例复测 9/9 通过，本轮新增严格目标基线采纳用例后为 10/10。

## 阶段 1

环境：macOS 27.0、Apple Silicon、pnpm 11.9.0、项目级 Node 24.21.0、PostgreSQL 15.18、Chromium 153。系统全局 Node 26.0.0 保持不变。

| 项目 | 命令 | 结果 | 证据/说明 |
|---|---|---|---|
| 依赖安装与锁定 | `pnpm install --frozen-lockfile=false` | 通过 | 固定 Next 16.3.5、React 19.3.0、Prisma 7.10.0、Better Auth 1.7.5、Vitest 5.0.1、Playwright 1.63.0；peer 检查无冲突 |
| Node 运行时 | `pnpm exec node --version`、`pnpm exec prisma --version` | 通过 | 均实际使用 Node 24.21.0；系统 Node 26 未被替换 |
| Prisma schema | `prisma format`、`prisma validate`、`prisma generate` | 通过 | schema 有效并生成 Prisma Client |
| 开发库迁移 | `prisma migrate dev`、`prisma migrate deploy` | 通过 | `badminton_tournament_dev` 应用 2 次迁移 |
| 种子幂等 | 两次 `pnpm run db:seed` 加前后计数 | 通过 | 两次均保持 1 赛事、2 比赛、6 选手、2 账号 |
| 静态检查 | `pnpm run lint`、`pnpm run typecheck` | 通过 | ESLint 0 错误/警告；TypeScript 0 错误 |
| 单元与集成测试 | `pnpm test` | 12/12 通过 | 自动验证 `_test` 数据库；覆盖规则配置、数据库隔离、生产示例配置拒绝、角色/指派、唯一活动控制会话和公开字段 |
| 生产构建 | `pnpm run build` | 通过 | Next.js 16.3.5 编译、类型检查和路由生成成功 |
| Chromium 流程 | `pnpm run test:e2e` | 5/5 通过 | 公开页刷新持久化、未登录拒绝、关闭注册、管理员不能执裁、裁判只能控制本人比赛 |
| 默认端口启动 | `pnpm dev`、`curl /api/health`、`curl /` | 通过 | `0.0.0.0:3000` 在 158ms 就绪；健康检查返回数据库连接和服务端时间；首页 200 |
| 阶段 1 知识库同步 | dry-run、apply、再次 dry-run、逐文件哈希、Obsidian CLI read | 通过 | 初次 17 更新、13 未变、0 冲突；复核 30 个 `UNCHANGED`、清单 30、哈希差异 0，当前进度复读成功 |

首次浏览器复测因测试错误地期待公开页显示内部比赛代码而出现 1 条失败、其余 4 条未运行；修正为刷新后检查公开对阵后，完整 5 条全部实际运行并通过。没有把首次未运行的 4 条算作通过。

未运行/未完成：真实手机与平板人工验收、局域网实机访问、生产部署、备份恢复、完整计分和业务链、两台物理设备竞争、正式赛事规则验收。这些不能由当前自动化结果替代。

## 阶段 1 审查问题修复回归（Codex）

> 下列是 Codex 在隔离测试库上的修复和回归记录，不是 Claude 独立复验结论。

| 项目 | 实际命令 | 结果 | 证据/说明 |
|---|---|---|---|
| 修复前回归暴露 | 定向 Vitest/Playwright | 按预期失败 | 模板、缺失配置提示、结构化日志各 1 失败；拒绝审计 1 失败；健康时间、真实配置关闭注册、页面 403 各 1 失败 |
| 冻结锁文件安装 | `pnpm install --frozen-lockfile` | 通过 | `Already up to date`，pnpm 11.9.0；原阶段 1 首次安装的 `--frozen-lockfile=false` 保留为当时真实历史，不改写 |
| 静态检查 | `pnpm run lint`、`pnpm run typecheck` | 通过 | ESLint 和 TypeScript 均 0 退出 |
| 全量 Vitest | `pnpm test` | 16/16 通过 | 7 个测试文件；先确认库名 `badminton_tournament_test` 并迁移/种子 |
| 全量 Playwright | `pnpm run test:e2e` | 8/8 通过 | 包含真实测试配置下关闭注册、UTC 对时、管理页 403、公开页 404、拒绝无业务写入和套件清理 |
| 生产构建 | `pnpm run build` | 通过 | Next.js 16.3.5 编译、类型检查与路由生成通过；`authInterrupts` 为 Next 当前标记的实验功能 |
| 全新测试库种子 | 临时 `badminton_phase1_seed_regression_test` 两次 `prepare-test-database` | 通过 | 首次从空库创建 2 用户/2 密码凭据，第二次计数不变；验证后已删除临时库 |
| 测试库残留检查 | Prisma 计数查询 | 通过 | `scoringSessions=0`、相关 `auditLogs=0`、`sessions=0`、注册探针账号 `blockedUsers=0` |
| 知识库安全同步 | `node --test tests/sync-knowledge-base.test.mjs`、dry-run、apply、再次 dry-run | 通过 | 同步工具 10/10；首次 `update=10 unchanged=21 conflict=0`；应用 31 份受管理文档后复核 `unchanged=31 stale=0 conflict=0` |

未改变的边界：真实生产启动、真机/局域网、阶段 2/3 版本与租约命令、数据库 `timestamp without time zone` 的长期迁移方案均未验证或未实现。

## 阶段 1 独立复验后续修复（Codex）

> 本节保留 S1-010—S1-014 修复当时的 Codex 回归；这些问题随后已由 Claude 独立复验通过，见阶段 2 审查报告中的更正与汇总。

| 项目 | 实际命令 | 结果 | 证据/说明 |
|---|---|---|---|
| 修复前定向回归 | 定向 Vitest、生产启动进程测试 | 按预期失败 | 新口令校验 false；8 个并发拒绝写 8 条；文档测试 2 条失败；非法生产配置仍进入 Ready |
| 隔离确认 | `assertTestDatabaseUrl`、数据库名读取 | 通过 | 仅连接 `badminton_tournament_test` |
| 定向 Vitest | `dotenv -e .env.test -- vitest run` 三个新增/受影响文件 | 9/9 通过 | 口令轮换、并发审计抑制和文档一致性均通过 |
| 冻结安装 | `pnpm install --frozen-lockfile` | 通过 | `Already up to date` |
| 静态检查 | `pnpm run lint`、`pnpm run typecheck` | 通过 | 均为 0 退出 |
| 全量 Vitest | `pnpm test` | 20/20 通过 | 9 个测试文件；种子轮换结束恢复原测试口令 |
| 全量 Playwright | `pnpm run test:e2e` | 8/8 通过 | 原阶段 1 登录、权限、状态码和清理链无回归 |
| 生产启动门禁 | `node --test tests/server/production-startup-security.test.mjs` | 2/2 通过 | 非法配置在调用 Next 前非零退出；合法配置启动并通过健康检查 |
| 同步工具测试 | `node --test tests/sync-knowledge-base.test.mjs` | 10/10 通过 | 冲突、符号链接、未知文件和无删除门禁保持 |
| 测试库残留 | Prisma 只读计数 | 通过 | `sessions=0`、`scoring_sessions=0`、`audit_logs=0` |
| Claude 报告哈希 | `shasum -a 256` | 不变 | 原审查 `bec9e433…69ea7`；独立复验 `f10ef209…30fc2e` |

生产部署、真机/局域网、外部探针和接口级总量限流仍未验证；不能由本机进程测试替代。

## 阶段 2：纯规则引擎（Codex）

> 下列是 Codex 对纯 TypeScript 引擎的实现和回归，不是 Claude 独立复验结论，也不代表阶段 3 的服务端持久化或裁判页面已完成。

| 规则/风险 | 测试名称或命令 | 实际结果 |
|---|---|---|
| 三个演示 profile | `规则配置骨架` | 传统 21、替代 3×15、单局 21 参数与校验 3/3 通过 |
| 双打发接发 | `逐分执行双打黄金轨迹`、`支持 B 方首发和不同首接球员` | 原提示词每步比分、发球员、接发球员、双方 R/L 全部通过 |
| 单打与计分边界 | 单打奇偶、20:20、29:29、14:14、20:20 | 21/2/30 与 15/2/21 边界全部通过 |
| 间歇和换边 | 单局 11、3×15 的 8、局间义务、漏换边 | 每个触发点一次；补换边保留比分 |
| 局间设置 | `下一局重新选择首发/首接且不重新抛币` | 通过；上一局记录和抛币记录保持 |
| 撤销 | 普通分、阈值分、局末、赛末、实际换边 | 比分、胜局、发接发、R/L 恢复；实际换边生成物理端核对事项 |
| 更正边界 | R/L、发接发、物理端、完整比分状态 | 独立命令、原因必填、非法负分/非对角状态拒绝且零修改 |
| 特殊结果 | 暂停/恢复、RET、提交/确认/重开 | 保留真实比分，不补造未打局 |
| 幂等和重放 | 同 ID 同/异内容、JSON 往返、事件版本 | duplicate/拒绝语义和确定性重放通过 |
| 不变量辅助 | 固定种子随机合法回合 | 球员身份、比分非负、发接发归属保持 |

实际质量命令：

| 项目 | 命令 | 结果 |
|---|---|---|
| 修复前阶段 2 回归 | 定向 Vitest | 按预期失败：三个 profile 缺失、`match-engine` 模块不存在 |
| 阶段 2 定向 | `pnpm exec vitest run tests/domain/rule-profile.test.ts tests/domain/match-engine.test.ts` | 26/26 通过 |
| 全量 Vitest | `pnpm test` | 44/44 通过，10 个测试文件 |
| 静态检查 | `pnpm run lint`、`pnpm run typecheck` | 通过 |
| 浏览器回归 | `pnpm run test:e2e` | 8/8 通过 |
| 生产构建与启动门禁 | `pnpm run test:production-startup-security` | build 通过；进程测试 2/2 |

未实现/未测试：数据库事件事务、`expectedVersion` 并发、控制租约心跳、裁判长接管、响应丢失、完整裁判 UI、真实设备和正式赛事规则采用。

## 阶段 2 独立审查阻塞修复（Codex，2026-09-19）

> Claude 原始审查报告保持原文。本节只记录 S2-001—S2-003 修复后的 Codex 回归，状态为“已修复，待复验”，不代表 Claude 已复验通过。

| 编号 | 先失败的回归 | 修复后结果 | 当前边界 |
|---|---|---|---|
| S2-001 | 25:3 仍为进行中、胜局与历史局矛盾、11 分缺少阈值义务原本均被接受 | 三类矛盾均拒绝；完整阈值义务可接受 | 审批和数据库事务仍属阶段 3 |
| S2-002 | 有 `CHANGE_ENDS` 时裸物理端更正原本被接受，随后确认会二次交换 | 裸更正拒绝；显式引用义务后消费且端位只改变一次 | UI 必须显式询问现场事实 |
| S2-003 | 篡改最后事件快照为 99:99 后重放返回 99:99 | 逐事件重新执行命令；快照篡改抛错；JSON 与撤销事件重放一致 | 尚无数据库持久化或外部签名 |

| 项目 | 实际命令 | 结果 |
|---|---|---|
| 修复前定向回归 | `pnpm exec vitest run tests/domain/match-engine.test.ts` | 新增 3 条用例全部按预期失败，原 23 条通过 |
| 修复后定向回归 | 同上 | 26/26 通过 |
| 安装锁定 | `pnpm install --frozen-lockfile` | 退出码 0，锁文件无需变化 |
| 静态检查 | `pnpm run lint`、`pnpm run typecheck` | 退出码 0 |
| 全量 Vitest | `pnpm test` | 47/47 通过，10 个测试文件；连接 `badminton_tournament_test` |
| 浏览器回归 | `pnpm run test:e2e` | 8/8 通过 |
| 生产构建 | `pnpm run build` | 通过，11 条路由生成 |
| 生产启动门禁 | `pnpm run test:production-startup-security` | build 通过，进程测试 2/2 |
| 测试库残留 | Prisma 只读计数 | `sessions=0`、`scoring_sessions=0`、`audit_logs=0` |

S2-004（连续向前撤销语义）和 S2-005（待老师确认问题补充）本轮未修，按 Claude 首轮审查结论不构成阶段 3 前置条件。阶段 3 未开始。

## 阶段 2 第二轮复验后 S2-007 修复（Codex，2026-09-19）

> Claude 第二轮独立复验判定 S2-002、S2-003、S2-006 通过，S2-001 部分通过并另立 S2-007。本节只记录 S2-007 修复后的 Codex 回归，不代表 Claude 已复验通过。

| 场景 | 修复前 | 修复后 |
|---|---|---|
| `SUBMITTED` + 比分更正 | accepted | `result_locked`，聚合不变 |
| `CONFIRMED` + 四类 `CORRECT_*` | accepted | 全部 `result_locked`，聚合不变 |
| 未抛币 + 比分更正 | accepted，可跳过开局链 | `score_correction_not_allowed` |
| 特殊结果 + 比分更正 | accepted，互斥事实并存 | `score_correction_not_allowed` |
| 暂停 + 比分更正 | accepted，留下暂停残留字段 | `score_correction_not_allowed` |
| 刚结束一局 + 比分更正 | accepted | 继续允许 |
| `CONFIRMED → REOPEN_RESULT` 后更正 | 原门禁不存在 | 明确允许受控重开后的待提交状态更正 |

| 项目 | 实际命令 | 结果 |
|---|---|---|
| 修复前定向回归 | `pnpm exec vitest run tests/domain/match-engine.test.ts` | 新增 2 条用例失败，既有 26 条通过 |
| 受控重开正向回归 | 定向单用例 | 加入白名单前按预期失败 |
| 修复后定向回归 | `pnpm exec vitest run tests/domain/match-engine.test.ts` | 28/28 通过 |
| 静态检查 | `pnpm run lint`、`pnpm run typecheck` | 通过 |
| 全量 Vitest | `pnpm test` | 49/49 通过，10 个测试文件 |
| 浏览器回归 | `pnpm run test:e2e` | 8/8 通过 |
| 生产构建 | `pnpm run build` | 通过 |
| 生产启动门禁 | `pnpm run test:production-startup-security` | 2/2 通过 |
| 测试库残留 | Prisma 只读计数 | `sessions=0`、`scoring_sessions=0`、`audit_logs=0`、`match_events=0` |

S2-007 已在第 3 轮独立复验中通过。S2-008 已登记未修复，按 Claude 结论不阻塞阶段 3。

## 阶段 2 第三轮复验后 S2-009 修复（Codex，2026-09-19）

> Claude 独立复验确认 S2-007 通过并允许进入阶段 3，同时新发现非阻塞的 S2-009。本节只记录 Codex 对 S2-009 的实现和本地回归，不冒充 Claude 复验。

| 场景 | 实际结果 |
|---|---|
| 进行中误记 RET 后作废 | 比分、发接发、R/L、物理端和 phase 精确恢复 |
| 暂停、待办、已抛币未开局状态 | 均恢复到录入特殊结果前的原状态 |
| 直接覆盖特殊结果 | `special_outcome_already_recorded` |
| `SUBMITTED` / `CONFIRMED` 直接作废 | `special_outcome_cannot_be_invalidated` |
| `REOPEN_RESULT` 后作废 | accepted，回到特殊结果前状态 |
| 幂等与重放 | 同 ID 同内容 duplicate，异内容拒绝，确定性重放一致 |

| 项目 | 命令 | 结果 |
|---|---|---|
| 定向回归 | `pnpm exec vitest run tests/domain/match-engine.test.ts` | 31/31 通过 |
| 全量 Vitest | `pnpm test` | 52/52 通过，10 个测试文件 |
| 静态检查 | `pnpm run lint`、`pnpm run typecheck` | 通过 |
| 浏览器回归 | `pnpm run test:e2e` | 8/8 通过 |
| 生产构建与启动门禁 | `pnpm run test:production-startup-security` | build 通过，2/2 通过 |

S2-009 当前状态为“已实现并通过本地回归，待独立复验”。此项不阻塞阶段 3。

## 阶段 3：权威计分事务与裁判工作台（Codex，2026-09-19）

> 本节是本机 `_test` 数据库和浏览器设备模拟的自动化结果，不代表物理手机/平板、真实裁判或正式赛事规则验收。

| 项目 | 命令/范围 | 实际结果 | 证据/说明 |
|---|---|---|---|
| 数据库迁移 | `pnpm run db:migrate` 及测试库准备 | 通过 | 开发库与测试库均应用 `20260919061500_phase3_authoritative_scoring`，复查为 `Already in sync` |
| 阶段 3 集成测试 | `tests/server/phase-3-scoring.test.ts` | 8/8 通过 | 覆盖初始快照/哈希、幂等、并发版本、心跳/接管、提交/锁定、S2-009 持久化、篡改拒绝和事务回滚 |
| 全量 Vitest | `pnpm test` | 60/60 通过，11 个测试文件 | 使用 `badminton_tournament_test`；既有阶段 1/2 用例未删除或改写迁就新实现 |
| 全量 Playwright | `pnpm run test:e2e` | 11/11 通过 | 单打和双打真实 HTTP 全流程；响应中止后按原 ID 找回；平板控制端/手机只读端；离线禁写与旋转版本不变 |
| 静态检查 | `pnpm run lint`、`pnpm run typecheck` | 通过 | ESLint 和 TypeScript 均 0 退出 |
| 生产构建与启动门禁 | `pnpm run test:production-startup-security` | build 通过，3/3 通过 | 新增：生产环境显式拒绝测试响应延迟开关 |
| 同步工具测试 | `node --test tests/sync-knowledge-base.test.mjs` | 10/10 通过 | 原冲突、越界、符号链接、无删除和严格采纳门禁保持 |
| 测试库残留 | Prisma 只读计数 | 全部为 0 | `sessions`、`scoring_sessions`、`audit_logs`、`match_events`、`match_snapshots`、`result_revisions` 均无残留 |
| 浏览器设备模拟截图 | Playwright Chromium | 已生成 | `artifacts/phase3-browser-simulated/tablet-1024x768.png`、`mobile-390x844.png`；明确不是物理真机截图 |

本轮未测试：物理手机/平板、局域网现场网络切换、真实裁判连续操作、正式赛事规则采用、生产部署和跨进程高压并发。报名、编排、排名影响与成绩册仍属于后续阶段。
