---
title: 需求验收追踪矩阵
stage: 3
status: current
updated: 2026-09-24
tags:
  - 羽毛球赛事管理系统
  - 需求追踪
---

# 需求验收追踪矩阵

状态只能使用“已完成、已规划、未实现、未测试、待核验、后续版本”。实现和测试位置要在后续阶段填入真实路径，不能预填不存在的代码。

| 需求 ID | 需求 | 验收标准 | 实现位置 | 测试 | 文档 | 状态 | 待确认项 |
|---|---|---|---|---|---|---|---|
| SYS-001 | 中文响应式网页 | 手机、平板、电脑共享权威后端 | `/officiating/[matchCode]`、`ScoringWorkbench`、权威计分 API | Playwright 手机竖屏/平板横屏浏览器设备模拟通过；Chromium 375/390/1024 宽度与 200% 字号视觉实测通过；真机未测试 | [产品范围](产品需求与范围.md) | 已完成 | 真机设备范围仍待验收 |
| SYS-002 | 可启动工程和健康检查 | 锁文件可重复安装；应用构建/启动；数据库健康返回服务端时间 | `package.json`、`src/app/api/health/route.ts` | frozen-lockfile、lint、typecheck、build 通过；Playwright 对时回归通过 | [本地启动](../07-使用部署/本地启动与手机访问.md) | 已完成 | 无 |
| OPS-001 | 隔离公网演示部署 | 不占用或改动既有项目；只暴露一个应用端口；数据库不映射到主机；重启后健康 | 演示服务器上的独立部署目录、独立 Compose 项目/网络/卷、TCP 80 | 公网健康、首页和 `/board` 200；三身份登录/对应页面均 200；应用容器 healthy；同机既有项目首页仍 200；本地 Vitest 183/183、Playwright 54/54、生产启动安全 4/4 | [备份恢复与部署](../07-使用部署/备份恢复与部署.md) | 已完成 | 仅 HTTP 演示；HTTPS、域名、反向代理、恢复演练和正式生产验收未完成 |
| DEMO-001 | 人工直采模拟数据与只读比赛看板 | 至少覆盖待开赛、结束待提交、已提交待复核和已锁定；正常比分与特殊结果可见；至少四场为 0 事件、0:0、已指派裁判的全新比赛；公开页不提供写入口 | `prisma/seed.ts`、`src/app/board/page.tsx`、`src/app/officiating/page.tsx`、`src/components/match-board-auto-refresh.tsx` | `manual-capture-board.test.ts`、公开字段回归和 Playwright 看板/裁判列表用例通过；生产库 17 场、16 名匿名选手、55 条事件；四场 FRESH 均为 `READY/version=0/events=0/0:0/activeOfficial=1`；公网 `/board` 200 | [测试记录](../05-测试验收/测试记录.md) · [备份恢复与部署](../07-使用部署/备份恢复与部署.md) | 已完成 | 仅人工测验夹具；不是阶段 4 报名编排、真实公开发布或正式赛事数据 |
| AUTH-001 | 五类角色和真实登录 | 未登录及越权请求由服务端拒绝 | `src/server/auth/`、Better Auth 表、`RoleAssignment` | Vitest/Playwright 通过 | [API 与权限](../02-架构设计/API与权限.md) | 已完成 | 校园统一身份接入属后续选择 |
| AUTH-002 | 主裁判限本人获授权比赛 | 知道 URL 也不能写其他比赛 | `authorization.ts`、`OfficialAssignment`、控制 API | 管理员和未指派比赛均 403；拒绝后控制会话不变且写入 `DENIED` 审计；本人比赛 201 | [API 与权限](../02-架构设计/API与权限.md) | 已完成 | 临时换裁判流程 |
| AUTH-003 | 示例身份生产安全 | 公开注册关闭；示例账号配置或示例密钥不能用于生产启动 | `runtime.ts`、`auth.ts`、`prisma/seed.ts` | 关闭注册浏览器用例通过；公网演示通过一次性种子创建身份，运行容器未携带三类示例密码且生产门禁保持启用 | [技术决策](../02-架构设计/技术选型与决策.md) | 已完成 | 模拟参赛选手仅为无赛事角色的只读登录身份，未与 `Participant` 建立个人业务绑定 |
| EVT-001 | 赛事、项目和组别配置 | 支持男单/女单/男双/女双/混双配置 | `/management/new` 四步向导、`tournament-admin-service.ts`（`createTournament`/`addCompetition`）；项目代码可建组别（如 MS-A）；创建者自动获赛事 ADMIN；新建赛事须平台级 `User.systemRole=SYSTEM_ADMIN` | `registration.test.ts` 赛事创建 3 组（权限、无效输入、时区窗口与阶段）；`registration.spec.ts` 浏览器向导建赛 | [数据库](../02-架构设计/数据库与实体关系.md) · [API 与权限](../02-架构设计/API与权限.md) | 已完成 | 实际项目与组别设置待组织者确认；已建项目暂不支持删除 |
| EVT-002 | 规则配置和比赛快照 | 赛事→组别→阶段继承；发布冻结；每场保存 `configHash` 快照 | `RuleProfileRevision`、`MatchRuleSnapshot`、模拟种子和纯引擎冻结输入已完成；继承服务未实现 | 快照/哈希和引擎冻结测试通过；RULE-V1-003 的跨阶段服务未测试 | [执行规则](../03-竞赛规则/赛事执行规则-v1.0.md) | 未实现 | 当届采用记录 |
| EVT-003 | 三个计分预设和自定义校验 | 21、替代 3×15、单局 21 参数正确；非法阈值拒绝 | `rule-profile.ts` 三个演示配置和校验已实现 | RULE-V1-001/002 纯引擎回归通过，待独立复验 | [执行规则](../03-竞赛规则/赛事执行规则-v1.0.md) | 已完成 | 正式赛事采用记录 |
| REG-001 | 公众在线报名与材料审核 | 后续版本可接入，不阻塞 V1 后台录入 | 阶段 4-A 只实现受限替代：匿名邀请链接（见 REG-005），不开放公众注册、不收材料、不做在线资格审核 | 见 REG-005 | [需求差异](需求补充-v1.0差异记录.md) | 后续版本 | 材料审核与公众自助报名仍属后续版本 |
| REG-002 | 双打录入与搭档冻结 | 恰好两名不同成员；抽签发布后不得改姓名换人 | 报名与 Entry 均由数据库约束触发器保证单打 1 人/双打 2 人；A+B 与 B+A 同一去重键；同一人同项目只能在一个报名单位（`entry_members(competitionId, participantId)` 唯一）；`Entry.frozenAt` 或已有比赛时拒绝通过与撤回；改名与换人区分（改名留审计，换人只能撤回重报）。抽签发布时设置冻结属 4-B | `registration.test.ts` 双打顺序颠倒、同人两组合、冻结后撤回/通过被拒、数据库兜底 | [执行规则](../03-竞赛规则/赛事执行规则-v1.0.md) | 未实现 | 抽签发布冻结与发布后换人规程属 4-B |
| REG-003 | 手工录入和 Excel/CSV 导入 | 预览新增/重复/错误；重复导入幂等；重名不自动合并 | 后台手工录入；CSV 模板下载 → 预览（行号、错误、重复、同名提示）→ 确认；UTF-8 与 GB18030 自动识别；≤256 KiB/500 行；公式样式单元格拒绝；原文件不落盘；同一内容摘要只能导入一次；确认时重算，数据变化即拒绝；整份单事务 | `registration-rules.test.ts` CSV/计划 8 项；`registration.test.ts` 导入 3 组；`registration.spec.ts` 浏览器错误预览与确认导入 | [API 与权限](../02-架构设计/API与权限.md) | 已完成 | 不直接解析 .xlsx（需在表格软件中另存为 CSV）；模板字段待组织者确认 |
| REG-004 | 跨项目身份与冲突检查 | 用内部运动员 ID 展开单双打成员，检查重叠和休息 | 身份部分已完成：`Participant` 按赛事隔离，学号赛事内唯一，审核时按学号自动关联同一人、同名无学号必须人工确认；同一人可跨项目关联同一 `Participant`。赛程重叠与休息检查未实现（4-C） | `registration.test.ts` 同名不同人、跨项目同一人、学号冲突 | [执行规则](../03-竞赛规则/赛事执行规则-v1.0.md) | 未实现 | 重叠与休息检查属 4-C |
| REG-005 | 匿名邀请链接报名 | 不开放公众注册；令牌只存摘要；报名窗口外拒绝；限流；只生成待审核报名；不收集身份证 | `registration-invite-service.ts`、`/register/[token]`、`POST /api/register/[token]`；256 位令牌只返回一次；无效/停用/过期/名额满统一提示；按来源指纹（HMAC）与按邀请总量双层限流；同意声明与蜜罐字段；`noindex`、`no-referrer` | `registration.test.ts` 邀请 2 组（摘要、窗口、同意、蜜罐、名额、限流、停用、过期、不建账号）；`registration.spec.ts` 手机 390 宽提交与回执 | [API 与权限](../02-架构设计/API与权限.md) | 已完成 | 无反向代理时 `x-forwarded-for` 可伪造，按来源限流只是尽力而为，真正上限是按邀请的总量与短时窗口 |
| REG-006 | 报名审核生成 Entry | 审核通过才生成 Entry；驳回/撤回必须写原因；并发处理不重复；失败不留半份 | `registration-service.ts` `reviewRegistration`/`batchApproveRegistrations`/`renameParticipant`；`/management/[slug]/registrations`；赛事行锁 + `expectedVersion`；撤回已通过报名删除未编排 Entry 并保留报名记录 | `registration.test.ts` 报名与审核 8 组；`registration.spec.ts` 同名确认与批量结果 | [数据库](../02-架构设计/数据库与实体关系.md) | 已完成 | 到 Entry 为止，不生成比赛（抽签属 4-B） |
| EVT-004 | 赛事阶段推进与门户发布 | 阶段由组织者显式推进并审计；开赛不在 4-A；发布需日期和至少一个项目 | `transitionTournamentPhase`（筹备→报名中→截止，截止→重开须原因）、`publishTournament`、`updateTournamentSettings`（按赛事时区解释报名窗口） | `registration.test.ts` 阶段与发布；`registration-rules.test.ts` 时区换算 | [API 与权限](../02-架构设计/API与权限.md) | 已完成 | 进入 RUNNING/FINISHED 属后续阶段 |
| DRAW-001 | 分组、种子和软回避 | 按规模建议分组；种子有依据；无法回避时输出冲突 | 未实现 | RULE-V1-010 未测试 | [执行规则](../03-竞赛规则/赛事执行规则-v1.0.md) | 未实现 | 赛事采纳 |
| DRAW-002 | 晋级和签位映射 | 两组/四组映射正确；同组分半区；轮空不算实赛 | 未实现 | RULE-V1-011 未测试 | [执行规则](../03-竞赛规则/赛事执行规则-v1.0.md) | 未实现 | 三四名赛是否启用 |
| DRAW-003 | 小组排名与退出统计 | 按规则链可解释排序；退出排除全部贡献但历史保留 | 未实现 | RULE-V1-012—016 未测试 | [排名规则](../03-竞赛规则/分组排名与特殊结果.md) | 未实现 | 赛事采纳 |
| SCH-001 | 场地时间安排 | 发现同运动员、同场地、裁判及跨项目冲突 | 未实现 | RULE-V1-008 未测试 | [页面与流程](../04-交互设计/页面与流程.md) | 未实现 | 无 |
| SCH-003 | 休息和迟到门禁 | 默认 60 分钟；30—59 需裁判长批准；不足 30 拒绝；迟到不自动判负 | 未实现 | RULE-V1-018 未测试 | [执行规则](../03-竞赛规则/赛事执行规则-v1.0.md) | 未实现 | 赛事采纳 |
| SCH-002 | 裁判指派 | 指派与比赛授权一致并检查重叠 | `OfficialAssignment` 和本人指派读取已完成；排班冲突/管理 UI 未实现 | 本人/非本人授权测试通过；重叠未测试 | [API 与权限](../02-架构设计/API与权限.md) | 未实现 | 裁判配置方法 |
| OFF-001 | 本人比赛与接管 | 展示状态、控制设备和只读原因 | `/officiating`、`/officiating/[matchCode]`、控制/心跳/释放/接管 API | 指派授权、租约、裁判长接管、旧设备失效和双视口只读同步通过 | [裁判端规范](../04-交互设计/裁判端交互规范.md) | 已完成 | 真机接管操作仍未测试 |
| OFF-002 | 开赛抛硬币 | 胜方可选先发、先接或场地；刷新不重抽；无效/修正留痕 | 纯引擎、命令事务及裁判工作台 | 纯引擎重放、真实 HTTP 开局及刷新路径通过 | [裁判端规范](../04-交互设计/裁判端交互规范.md) | 已完成 | 正式现场规程仍待核验 |
| OFF-003 | 每局开局设置 | 记录首发方、发球员、接发球员和场地端 | 纯引擎、权威命令服务、裁判工作台 | 单双打开局、下一局设置和真实 HTTP 完整流程通过；双打选择非名单第一人为首发/首接并刷新保持通过 | [双打发接发](../03-竞赛规则/计分与双打发接发.md) | 已完成 | 现场确认步骤待人工验收 |
| OFF-004 | 单双打逐分记分 | `+1` 后给出权威比分及下一发接发 | `RALLY_WON`、命令事务、`ScoringWorkbench` | 单双打真实 HTTP 完整流程及浏览器控制端通过 | [双打发接发](../03-竞赛规则/计分与双打发接发.md) | 已完成 | 正式计分配置待核验 |
| OFF-005 | 双打发接发与左右分配 | 每一分后正确展示谁发给谁和逻辑发球区 | 纯引擎、权威快照、`buildCourtViewModel`、Diagram A 的 SVG 场地图 | 双打黄金轨迹、END_1/END_2 × 正常/翻转 投影矩阵、自动换位和真实 HTTP 流程通过；2026-09-20 追加需求包九步验收轨迹与两个手动更正场景逐步断言，引擎输出完全一致 | [双打发接发](../03-竞赛规则/计分与双打发接发.md) | 已完成 | 正式规则采用待核验 |
| OFF-006 | 手动左右调整 | 更正逻辑位置时记录原因且不凭空改比分 | 独立更正命令、预览 API、原因确认对话框 | 更正后比分不变、服务端预览及浏览器入口通过 | [换边撤销与更正](../03-竞赛规则/换边撤销与更正.md) | 已完成 | 正式审批边界待核验 |
| OFF-007 | 物理换边与视角翻转 | 两种操作状态独立且各自幂等 | 服务端物理端命令；`obligationRelationship`；设备本地屏幕视角 | S2-008 的 `FULFILL` / `KEEP_PENDING`、错误待办、旧事件重放和 HTTP 事务已独立复验通过；换边后队伍与比分面板移动但分值归属不变；翻转不改服务器版本；2026-09-20 追加断言：换边前后「左侧换位／右侧换位」的可访问名随画面队伍互换，证明按钮不绑定固定 A/B | [换边撤销与更正](../03-竞赛规则/换边撤销与更正.md) | 已完成 | 真机场地图方向待完成 |
| OFF-008 | 撤销和更正减分 | 恢复所有关联状态且保留审计历史 | 追加撤销、状态更正、预览 API 和事件/审计持久化 | 普通分、阈值、局末、赛末、换边和前后差异回归通过；场地图“−”只打开服务器预览，取消不写事件/不变版本 | [换边撤销与更正](../03-竞赛规则/换边撤销与更正.md) | 已完成 | 非最近一分审批规程待核验 |
| OFF-009 | 间歇、暂停和异常结局 | NORMAL/WO/RET/DSQ/SUSPENDED/ABANDONED/BYE 独立且不伪造比分 | 纯引擎、权威事务、工作台；含 S2-009 作废通路 | 特殊结果不补造比分；S2-009 的 4 种前状态 × 5 类结果、精确恢复、幂等、重放和服务端持久化已独立复验通过 | [特殊结果](../03-竞赛规则/分组排名与特殊结果.md) | 已完成 | 裁判长现场作用范围待核验 |
| OFF-010 | 结果提交、复核和锁定 | 主裁判提交后只读；裁判长退回或锁定；管理员不能直接改分 | `ResultRevision`、命令事务和工作台复核视图 | 主裁判提交、裁判长锁定、纯 ADMIN 拒绝和实际角色审计通过 | [裁判端规范](../04-交互设计/裁判端交互规范.md) | 已完成 | 退回现场流程待人工验收 |
| REL-001 | 命令幂等 | 同 `commandId` 同内容只生效一次，不同内容拒绝 | `MatchEvent` 唯一键、规范化摘要和事务重读 | 并发同命令仅一事件；异载荷复用 409 | [API 与权限](../02-架构设计/API与权限.md) | 已完成 | 无 |
| REL-002 | 乐观并发 | 过期 `expectedVersion` 明确返回冲突 | 命令事务、`MatchSnapshot` 和 `Match.version` | 两设备同版本仅一方成功，另一方 409 并获权威版本 | [API 与权限](../02-架构设计/API与权限.md) | 已完成 | 冲突文案待真机验收 |
| OFF-011 | 场地化裁判工作台布局 | 横向模拟场地 → 左/右换位与唯一换边 → 大比分 → 双方加分与更正；发球者唯一羽毛球标记、接发者明确标记、长姓名不截断 | `src/ui/court-geometry.ts`（Diagram A 场线）、重写的 `court-console.tsx`、`displayedLeftSide/RightSide` 投影、次要操作折叠抽屉 | lint/typecheck/build 通过；`pnpm test` 114/114；Playwright 43/43；390/820×1180/1180×820/1440×1000/320 五视口实拍 before/after 各 13 张，无根级横向溢出 | [场地化裁判工作台](../04-交互设计/场地化裁判工作台.md) · [审查与验收](../05-测试验收/裁判工作台改造-审查与验收.md) · [源码审查修复V3](../05-测试验收/阶段3-源码审查修复V3.md) | 已完成 | 2026-09-21 追加两轮聚焦收敛（局数进比赛栏、压缩重复信息、消除空白控制条），`pnpm test` 153/153、Playwright 47/47，截图另存 `v3-before`/`v3-after` 各 13 张；视觉是否满意由用户确认；真机与真人裁判试用仍未测试 |
| REL-006 | 局中阈值义务一次性（R3-001） | 间歇与决胜局换边在一局内只按规则发生一次；撤销、完整更正与已实际换边的关系明确；阈值来自冻结快照 | `match-engine.ts` 的 `thresholdObligationsIssued`、`thresholdObligationSpecs`、`applyRally`、`applyReplacement`；引擎版本升到 v2 | 新增 `tests/domain/threshold-obligations.test.ts` 修复前 10 红 4 绿、修复后 15/15 通过，含引擎 v1 历史事件重放升级 | [换边撤销与更正](../03-竞赛规则/换边撤销与更正.md) · [源码审查修复V3](../05-测试验收/阶段3-源码审查修复V3.md) | 已完成 | 仅开发自测，未独立复判 |
| REL-007 | 权威快照单调采用（R3-002） | 历史回执可核销命令，但界面版本与比分不得倒退，也不得把过期数据标成新鲜 | `src/ui/authoritative-snapshot.ts`；`scoring-workbench.tsx` 的 `applyAuthoritativeSnapshot` 单一入口 | 纯模块 6/6；浏览器注入修复前比分由 2 倒退为 1（30 次采样），修复后全为 2 | [裁判端规范](../04-交互设计/裁判端交互规范.md) · [源码审查修复V3](../05-测试验收/阶段3-源码审查修复V3.md) | 已完成 | 真机网络乱序未验证 |
| REL-008 | 明确拒绝与结果未知分离（R3-003） | 只有 4xx 且带结构化错误码才清除待确认命令；5xx/超时/断网/畸形 200 保留原 commandId；本机存储损坏进入受控恢复 | `src/ui/command-outcome.ts`、`src/ui/pending-command-store.ts`、重写的 `submitEnvelope`/`reconcilePendingCommand`/`retryPendingCommand` | 纯模块 15/15；浏览器注入 4/4，修复前 502 与畸形 200 无恢复面板、损坏记录抛未捕获异常 | [裁判端规范](../04-交互设计/裁判端交互规范.md) · [源码审查修复V3](../05-测试验收/阶段3-源码审查修复V3.md) | 已完成 | 真机后台节流与多标签页真机未验证 |
| REL-009 | 实际结束时间与提交/复核时间分离（R3-004） | 提交与复核不得覆盖实际终场时刻；受控重开保留；误记终场被作废后清除；未开赛的 WO 不伪造开赛时刻 | `scoring-command-service.ts` 的 `ENDED_PHASES` 与 `endedAtProjection` | `tests/integration/phase-3-scoring.test.ts` 受控时钟用例：10:00/10:05/10:10 后 `endedAt` 仍为 10:00，修订时间独立 | [执行规则](../03-竞赛规则/赛事执行规则-v1.0.md) · [源码审查修复V3](../05-测试验收/阶段3-源码审查修复V3.md) | 已完成 | 后续时长与最短休息排程尚未实现 |
| REL-010 | 目标设备安全上下文能力（R3-005） | 进入可写界面前检测安全随机源；非安全上下文有受审查兼容路径与 HTTPS 提示；缺能力时只读并说明原因 | `src/ui/secure-command-id.ts`；`scoring-workbench.tsx` 的能力门禁与提示 | 能力检测 5/5 通过；兼容 UUID 符合 RFC 4122 第 4 版且不使用 `Math.random` | [本地启动与手机访问](../07-使用部署/本地启动与手机访问.md) | 未测试 | **真机 iPhone/iPad 未连接**；HTTPS、反向代理与跨设备认证来源未配置 |
| OFF-012 | 抛币开局完整选择（RV3-002） | 胜方选发球或场地端后，负方可自行选择剩余类别；所有选项绑定真实 A/B 与场地端；取消不提交、确认前不发命令 | `scoring-workbench.tsx` 的 `CoinTossForm` 分步联动表单；引擎 `deriveCoinToss` 不变 | 领域 4 类组合 4/4；浏览器 2 条新用例：A 胜选先接且 B 选 END_1、B 胜选 END_1 且 A 选先发，均核对 `servingSide`、`physicalEnds` 与 `coinToss` 事件；截图 5 视口无横向溢出 | [70fff5e 复审与修复记录](../05-测试验收/70fff5e复审与修复记录.md) | 已修复待独立复验 | 真机与真人裁判未试用；未做抛币动画（不需要） |
| OFF-013 | 局末/赛末误点撤销入口（RV3-004） | 结束待提交相位提供显著且受控的撤销入口；只放开得到结束分的一方；已提交/已锁定/已开始下一局仍受原有控制 | `court-console.tsx` 的 `endingScorerSide` / `undoAllowedForSide`；`PhaseActions` 的 `ending-undo-entry` | 浏览器端到端：21:19 → 服务器预览 → 确认 → 20:19、`IN_PROGRESS`、`UNDO_LAST_REVERSIBLE` 事件恰 1 条；另一方“−1”仍禁用 | [70fff5e 复审与修复记录](../05-测试验收/70fff5e复审与修复记录.md) · [换边撤销与更正](../03-竞赛规则/换边撤销与更正.md) | 已修复待独立复验 | 已实际换边后的撤销仍依赖人工核对现场端位 |
| REL-011 | 暂停期间处理待办后可正常恢复（RV3-003） | 暂停中可核对现场事项；恢复必须进入与当前待办一致的相位，不得卡在待办为空的 `OBLIGATIONS_PENDING` | `match-engine.ts` 的 `settledPhaseAfterObligations` 与 `RESUME_MATCH` 结算 | 审查草案 3 个规则档修复前全红（`expected 'OBLIGATIONS_PENDING' to be 'IN_PROGRESS'`）、修复后全绿；另补部分处理、物理端更正完成、未处理三条边界；`replayMatch` 逐条比对历史轨迹通过 | [换边撤销与更正](../03-竞赛规则/换边撤销与更正.md) · [70fff5e 复审与修复记录](../05-测试验收/70fff5e复审与修复记录.md) | 已修复待独立复验 | 真机长时间暂停未测试 |
| REL-012 | 比分更正与普通得分共用同一推导（RV3-006） | 更正后的待办、相位、胜局数一律由规则引擎按新比分推导；浏览器不得持有第二套竞赛算法；领域校验不放松 | `match-engine.ts` 的 `deriveCorrectedObligations` 与导出的 `deriveScoreCorrectionReplacement`；`previewCurrentAction` 改为调用它 | 新增 5 条用例：旧做法 10:8→11:8 被拒 `corrected_obligations_inconsistent`；共用推导后接受并生成间歇；11:8→10:8 撤回阈值事实且可再次触发；阈值已处理后更正另一方不重复生成；撤销遗留的物理端核对待办不被吞掉 | [换边撤销与更正](../03-竞赛规则/换边撤销与更正.md) · [70fff5e 复审与修复记录](../05-测试验收/70fff5e复审与修复记录.md) | 已修复待独立复验 | 跨局末误点仍走撤销路径而非完整更正 |
| DOC-004 | 截图证据不被测试无声覆盖 | 普通测试运行不得重写已记账的截图目录；清单与目录内容自洽可复验 | `tests/e2e/phase-3.spec.ts` 与 `referee-court-shots.spec.ts` 默认写入未提交的 `*local-run` 目录；`.gitignore` 相应条目 | 还原 HEAD 字节后旧清单仍 6/6 FAILED（改动前已存在的不一致）；重算后自校验 6/6 OK，旧清单保留为 `SHA256SUMS.superseded-2026-09-21`；复跑整套 e2e 后 `git status artifacts/` 无已提交截图被改 | [70fff5e 复审与修复记录](../05-测试验收/70fff5e复审与修复记录.md) | 已完成 | 重算只让清单与现存文件对齐，未复现旧清单字节 |
| REL-003 | 单场写入租约 | 新接管后旧设备不可继续写入 | `ScoringSession`、控制代次、心跳/释放/接管 API | 心跳、过期、裁判长接管和旧设备恢复写入均通过 | [API 与权限](../02-架构设计/API与权限.md) | 已完成 | 真机后台节流待验收 |
| REL-004 | 响应丢失和断网安全 | 待确认命令可用原 ID 查询；断网禁写 | 命令查询 API、浏览器待确认命令存储和只读状态 | 测试延迟/中止后用原 `commandId` 找回且事件仍只有一条；未知响应和离线均禁写，恢复后先对账；2026-09-21 追加 R3-003 的 502/畸形 200/存储损坏注入，均保留原 commandId | [裁判端规范](../04-交互设计/裁判端交互规范.md) | 已完成 | 真机网络切换待验收 |
| REL-005 | 开发与测试数据隔离 | 自动化拒绝非 `_test` 数据库，种子不接触真实库且可重复 | `database-safety.ts`、`.env.test.example`、测试准备脚本、种子 | 4 条隔离/缺失配置测试、模板键检查和全新 `_test` 库两次种子通过 | [本地启动](../07-使用部署/本地启动与手机访问.md) | 已完成 | 无 |
| RES-001 | 成绩复核与锁定 | 主裁判提交、裁判长锁定的身份/时间/版本可追踪 | `ResultRevision`、提交/复核实际角色与时间、状态投影 | RULE-V1-019 的提交/独立复核/锁定主路径通过 | [执行规则](../03-竞赛规则/赛事执行规则-v1.0.md) | 已完成 | 现场复核仍未测试 |
| RES-002 | 小组名次和解释 | 展示胜场、直接胜负、净胜局、净胜分和抽签依据 | 未实现 | RULE-V1-012—016 未测试 | [特殊结果](../03-竞赛规则/分组排名与特殊结果.md) | 未实现 | 无 |
| RES-003 | 锁定后修订与影响 | 新增版本；冻结未开赛链；已开赛链禁止自动替换 | 未实现 | RULE-V1-021 未测试 | [换边撤销与更正](../03-竞赛规则/换边撤销与更正.md) | 未实现 | 争议裁决 |
| RPT-001 | 成绩册导出与打印 | 只用锁定一致快照生成版本化文件，防公式注入 | 未实现 | RULE-V1-023 未测试 | [模板说明](../08-成果材料/成绩册与公开查询模板说明.md) | 未实现 | 学校正式模板 |
| RPT-002 | 公开查询和隐私 | 服务端白名单；不返回学号、联系方式、医疗和内部记录 | `src/reports/public-fields.ts`、`public-projection.ts`、`public-tournament-service.ts`、首页赛事列表、`/public/[slug]/schedule`、`/public/[slug]/matches/[matchCode]` 及三个公开 API；原「公开查询」页已下线重定向 | `public-results-projection.test.ts` 19 项（含 `CODES_ONLY` 不泄露姓名、未发布比赛被排除、引擎状态与更正理由不下发）、`public-portal.spec.ts` 13 项、`phase-1.spec.ts` 白名单用例通过 | [页面与状态清单](../04-交互设计/公开赛程与成绩中心-页面与状态清单.md) | 已完成 | 姓名/代表队由 `Tournament.namePolicy` 决定，默认 `CODES_ONLY`；裁判公开显示名仍未实现 |
| RPT-003 | 模板与产品能力隔离 | HTML/PDF/JSON 始终标记模拟，不计为应用实现或真实验收 | 文档已完成 | 文档边界已检查 | [模板说明](../08-成果材料/成绩册与公开查询模板说明.md) | 已完成 | 无 |
| UI-01 | 公开赛程与成绩中心界面补充 | 公开端与管理/裁判端隔离；赛程、详情、三维状态、路由、响应式、错误态和隐私发布边界明确；正式结果只来自权威服务 | UI-01-A/B 同前；**UI-01-C 已接入真实数据库**：组件迁到 `src/features/public-results/`，由 `public-projection.ts` 适配 Prisma 行，`Match.publishedAt` 作逐场发布边界，赛事时区折算自然日并对无效 IANA 时区 fail closed；`/public/preview/...` 保留为 flag 门禁的确定性夹具 | 公开预览 Playwright 24/24 保持绿；新增门户 Playwright 13/13 与投影 Vitest 19/19；375/390/768/1024/1440 五视口无根级横向溢出，证据在 `artifacts/phase4-portal/` | [界面规范](../04-交互设计/亚运会网站参考与本项目界面规范.md) · [页面与状态清单](../04-交互设计/公开赛程与成绩中心-页面与状态清单.md) | 已完成 | 本轮为开发自测，未独立复判；`ESTIMATED/DELAYED/AFTER_MATCH` 时间语义、裁判公开显示名、UI-01-D 的对阵/排名/名次/成绩册仍未实现 |
| PORTAL-001 | 公开赛事门户首页 | 未登录访客看到按生命周期分组的赛事列表；左侧海报缩略图，右侧赛事名称与信息 | `src/app/page.tsx`、`listPublicTournaments()`、`TournamentPoster`；`TournamentPhase` 由组织者显式推进并审计，不按当前时间推导 | `public-portal.spec.ts` 首页分组、海报回退与五视口用例通过；`public-results-projection.test.ts` 断言草稿赛事不出现 | [页面与状态清单](../04-交互设计/公开赛程与成绩中心-页面与状态清单.md) | 已完成 | 赛事创建与阶段推进的管理界面已在 4-A 完成（EVT-001、EVT-004） |
| PORTAL-002 | 赛事海报上传 | 只接受 PNG/JPEG/WebP 且以魔数为准；≤2 MiB；文件名服务端重新生成；仅 ADMIN/ORGANIZER 可传 | `poster-service.ts`、`POST /api/admin/tournaments/[slug]/poster`、迁移中的 `tournaments_poster_path_check` | `poster-upload.test.ts` 7 项通过（魔数嗅探、超限、空文件、角色门禁、赛事不存在、数据库约束挡非法路径） | [备份恢复与部署](../07-使用部署/备份恢复与部署.md) | 已完成 | 部署须为 `public/posters/` 挂数据卷，否则重建镜像后海报丢失；4-A 已在赛事后台补上传表单（仅管理员、报名相关阶段显示） |
| PORTAL-003 | 导航按会话与角色收敛 | 未登录访客不显示赛事管理、我的执裁和设置 | `src/app/layout.tsx` 服务端解析会话 + `src/server/auth/nav-context.ts` + `app-nav.tsx` | `phase-1.spec.ts`：未登录首页导航无三项入口；未登录直连 `/management`、`/officiating`、`/settings` 仍被重定向，海报 API 返回 401；裁判导航无「赛事管理」但直连仍 403 | [API 与权限](../02-架构设计/API与权限.md) | 已完成 | **隐藏链接不是授权**：服务端门禁未作任何放宽，导航只影响展示 |
| DOC-001 | 仓库知识库权威源 | 同步清单中的文档非空且链接可解析，数量不得硬编码 | `docs/knowledge-base/` | 同步测试、dry-run 和受管理文件清单动态核对 | [首页](../00-首页.md) | 已完成 | 无 |
| DOC-002 | 安全单向同步 | dry-run、冲突保护、哈希、备份、无删除和严格目标基线采纳测试通过 | `scripts/sync-knowledge-base.mjs` | 10/10 通过；真实目标哈希差异 0 | [备份恢复](../07-使用部署/备份恢复与部署.md) | 已完成 | 无 |
| DOC-003 | Vault 手工改动安全采纳 | 只有仓库与目标逐字相同且无其他变更时更新清单，不改目标正文 | `scripts/sync-knowledge-base.mjs` | 10/10 通过 | [备份恢复](../07-使用部署/备份恢复与部署.md) | 已完成 | 无 |
